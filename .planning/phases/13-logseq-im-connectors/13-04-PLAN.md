---
phase: 13-logseq-im-connectors
plan: 04
type: execute
wave: 3
depends_on: []
files_modified:
  - src/saw/connectors/feishu/__init__.py
  - src/saw/connectors/feishu/models.py
  - src/saw/connectors/feishu/connector.py
  - src/saw/connectors/feishu/token_manager.py
  - src/saw/connectors/feishu/events.py
  - src/saw/connectors/wecom/__init__.py
  - src/saw/connectors/wecom/models.py
  - src/saw/connectors/wecom/connector.py
  - src/saw/connectors/wecom/crypto.py
  - src/saw/db/feishu_models.py
  - src/saw/db/wecom_models.py
  - src/saw/api/feishu_webhook.py
  - src/saw/api/wecom_webhook.py
  - tests/unit/test_feishu_connector/test_models.py
  - tests/unit/test_feishu_connector/test_connector.py
  - tests/unit/test_feishu_connector/test_token_manager.py
  - tests/unit/test_wecom_connector/test_models.py
  - tests/unit/test_wecom_connector/test_connector.py
  - tests/unit/test_wecom_connector/test_crypto.py
autonomous: true
requirements:
  - FEIS-01
  - FEIS-02
  - FEIS-03
  - FEIS-04
  - FEIS-05
  - WECO-01
  - WECO-02
  - WECO-03
  - WECO-04
user_setup:
  - service: feishu
    why: "Feishu app installation and message ingestion"
    env_vars:
      - name: FEISHU_APP_ID
        source: "Feishu Admin Console -> App Credentials -> App ID"
      - name: FEISHU_APP_SECRET
        source: "Feishu Admin Console -> App Credentials -> App Secret"
    dashboard_config:
      - task: "Create Feishu App"
        location: "Feishu Admin Console -> App Management -> Create App"
      - task: "Enable Bot Capability"
        location: "Feishu App -> Bot & Capability -> Enable Bot"
      - task: "Configure Event Subscription"
        location: "Feishu App -> Event Subscription -> Request URL: https://your-domain/api/v1/webhooks/feishu"
      - task: "Subscribe to Events"
        location: "Feishu App -> Event Subscription -> Add Events: im.message.receive_v1, drive.file.created_v1"
  - service: wecom
    why: "WeCom bot webhook for message ingestion"
    env_vars:
      - name: WECO_WEBHOOK_URL
        source: "WeCom Group Chat -> Group Settings -> Group Bot -> Add Bot -> Copy Webhook URL"
      - name: WECO_ENCODING_AES_KEY
        source: "WeCom Admin Console -> App Management -> Your App -> Set API Reception -> Random Key Generation"
      - name: WECO_TOKEN
        source: "WeCom Admin Console -> App Management -> Your App -> Set API Reception -> Token"
    dashboard_config:
      - task: "Create Group Bot"
        location: "WeCom Group Chat -> Group Settings -> Group Bot -> Add Bot"
      - task: "Copy Webhook URL"
        location: "After creating bot, copy the webhook URL"
must_haves:
  truths:
    - "User can install Feishu app and receive messages via webhooks"
    - "System handles multi-tenant tokens (app_token + tenant_token)"
    - "System captures Feishu Wiki docs as content source"
    - "System handles Chinese content encoding correctly"
    - "User can configure WeCom bot webhook for message ingestion"
    - "System handles WeCom's message encryption (AES-256-CBC)"
    - "System respects platform rate limits"
  artifacts:
    - path: "src/saw/connectors/feishu/connector.py"
      provides: "FeishuConnector implementing UnifiedConnectorInterface"
      exports: ["FeishuConnector"]
    - path: "src/saw/connectors/feishu/token_manager.py"
      provides: "Multi-tenant token management"
      exports: ["FeishuTokenManager"]
    - path: "src/saw/connectors/wecom/connector.py"
      provides: "WeComConnector implementing UnifiedConnectorInterface"
      exports: ["WeComConnector"]
    - path: "src/saw/connectors/wecom/crypto.py"
      provides: "AES-256-CBC encryption/decryption for WeCom"
      exports: ["WeComCrypto", "verify_wecom_signature"]
  key_links:
    - from: "src/saw/connectors/feishu/connector.py"
      to: "src/saw/connectors/protocol.py"
      via: "implements UnifiedConnectorInterface"
      pattern: "class FeishuConnector.*Protocol"
    - from: "src/saw/connectors/feishu/events.py"
      to: "src/saw/connectors/message_handler.py"
      via: "MessageHandler.extract_from_feishu"
      pattern: "MessageHandler"
    - from: "src/saw/api/wecom_webhook.py"
      to: "src/saw/connectors/wecom/crypto.py"
      via: "POST endpoint calls WeComCrypto.decrypt"
      pattern: "WeComCrypto"
---

<objective>
Implement Feishu and WeCom connectors for Chinese enterprise IM platforms with multi-tenant token management, webhook event handling, and AES-256-CBC encryption for WeCom.

Purpose: Enable message ingestion from Feishu (with Wiki doc support) and WeCom with proper Chinese content handling and encryption.
Output: Complete Feishu and WeCom connectors integrated with existing MessageHandler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-logseq-im-connectors/CONTEXT.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/saw/connectors/protocol.py:
```python
@runtime_checkable
class UnifiedConnectorInterface(Protocol):
    platform_name: str
    supports_push: bool
    async def authenticate(self, config: dict) -> AuthResult: ...
    async def get_items(self, since: datetime | None = None) -> list[ConnectorItem]: ...
    async def put_item(self, item: ConnectorItem) -> ConnectorItem: ...
    async def delete_item(self, item_id: str) -> bool: ...
    def transform_to_claim(self, item: ConnectorItem) -> Claim: ...
    def transform_from_claim(self, claim: Claim) -> ConnectorItem: ...
```

From src/saw/connectors/message_handler.py:
```python
@dataclass
class ExtractedMessage:
    platform: str
    external_id: str
    content: str
    author: MessageAuthor
    context: MessageContext
    timestamp: datetime
    metadata: dict = field(default_factory=dict)
```

From src/saw/connectors/rate_limiter.py:
```python
class RateLimitManager:
    async def acquire(self, platform: str) -> None: ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Feishu models and database schema</name>
  <files>
    src/saw/connectors/feishu/__init__.py,
    src/saw/connectors/feishu/models.py,
    src/saw/db/feishu_models.py,
    tests/unit/test_feishu_connector/test_models.py
  </files>
  <behavior>
    - Test 1: FeishuTenantModel stores tenant_key and app_token
    - Test 2: FeishuMessageEvent validates message payload structure
    - Test 3: FeishuWikiDoc extracts doc_token, title, content
    - Test 4: FeishuConfig validates app_id and app_secret
    - Test 5: Chinese content preserved in model fields (UTF-8)
  </behavior>
  <action>
    Create Feishu connector package with Pydantic models and SQLAlchemy schema (FEIS-01~05):

    1. Create `src/saw/connectors/feishu/__init__.py` with exports

    2. Create `src/saw/connectors/feishu/models.py`:
       - `FeishuConfig`: app_id, app_secret
       - `FeishuTenant`: tenant_key, tenant_name, tenant_token (cached)
       - `FeishuMessage`: message_id, chat_id, msg_type, content, sender, create_time
       - `FeishuWikiDoc`: doc_token, title, content, folder_token, creator_id
       - `FeishuEvent`: event_type, event_id, event, header (with tenant_key)

    3. Create `src/saw/db/feishu_models.py`:
       - `FeishuTenantModel`: id, tenant_key (indexed), tenant_name, tenant_token_encrypted, expires_at
       - `FeishuChatModel`: id, tenant_key, chat_id, chat_name, chat_type, is_enabled
       - `FeishuWikiModel`: id, tenant_key, doc_token (indexed), doc_title, last_sync_at
       - Register in `src/saw/db/__init__.py`

    4. Write unit tests for all models

    Use Pydantic v2. Ensure UTF-8 encoding for Chinese content (FEIS-05).
  </action>
  <verify>
    <automated>pytest tests/unit/test_feishu_connector/test_models.py -v</automated>
  </verify>
  <done>
    - Feishu message and wiki models defined
    - SQLAlchemy models for multi-tenant persistence
    - Chinese content handling tested
    - All model tests passing (minimum 5 tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Feishu multi-tenant token manager</name>
  <files>
    src/saw/connectors/feishu/token_manager.py,
    tests/unit/test_feishu_connector/test_token_manager.py
  </files>
  <behavior>
    - Test 1: Token manager obtains tenant_access_token via API
    - Test 2: Tokens cached with expiry time
    - Test 3: Expired tokens refreshed automatically
    - Test 4: Multiple tenants managed separately
    - Test 5: Thread-safe token access with mutex
  </behavior>
  <action>
    Implement FeishuTokenManager for multi-tenant token handling (FEIS-03):

    1. Create `src/saw/connectors/feishu/token_manager.py`:

    2. `FeishuTokenManager`:
       - Constructor takes app_id and app_secret
       - `_tenant_tokens: dict[str, TenantToken]` - cache by tenant_key

    3. `async get_tenant_token(tenant_key: str) -> str`:
       - Check cache for valid token (not expired)
       - If expired or missing, fetch new token:
         - POST to `https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal`
         - Body: `{"app_id": ..., "app_secret": ...}`
       - Cache with expiry (expire - 60 seconds buffer)
       - Use asyncio.Lock for thread safety

    4. `async refresh_tenant_token(tenant_key: str) -> str`:
       - Force refresh regardless of cache state
       - Update cache

    5. Token storage:
       - Encrypt tenant_token before storing in database (AUTH-02)
       - Decrypt on load

    6. Write unit tests with mocked HTTP responses

    Per CONTEXT.md: Store app_token and tenant_token separately. Refresh both on expiry.
  </action>
  <verify>
    <automated>pytest tests/unit/test_feishu_connector/test_token_manager.py -v</automated>
  </verify>
  <done>
    - Multi-tenant token management working
    - Token caching and auto-refresh
    - Thread-safe token access
    - All token manager tests passing (minimum 5 tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement FeishuConnector and event handler</name>
  <files>
    src/saw/connectors/feishu/connector.py,
    src/saw/connectors/feishu/events.py,
    src/saw/api/feishu_webhook.py,
    tests/unit/test_feishu_connector/test_connector.py
  </files>
  <behavior>
    - Test 1: Connector implements UnifiedConnectorInterface
    - Test 2: authenticate() validates app credentials
    - Test 3: Event handler processes im.message.receive_v1 events
    - Test 4: Event handler processes drive.file.created_v1 (Wiki docs)
    - Test 5: Chinese content decoded correctly from JSON
    - Test 6: transform_to_claim() creates Claim with proper metadata
  </behavior>
  <action>
    Implement FeishuConnector with webhook event handling (FEIS-01, FEIS-02, FEIS-04, FEIS-05):

    1. Create `src/saw/connectors/feishu/connector.py`:

    2. `FeishuConnector(BaseConnector)`:
       - `platform_name = "feishu"`
       - `supports_push = False`
       - Uses `lark-oapi` SDK for API calls

    3. `authenticate(config: dict) -> AuthResult`:
       - Validate app_id and app_secret
       - Initialize TokenManager

    4. Create `src/saw/connectors/feishu/events.py`:

    5. `FeishuEventHandler`:
       - `handle_event(payload: dict) -> list[ExtractedMessage]`:
         - Route by event type
       - `handle_message_event(event: dict) -> ExtractedMessage`:
         - Extract message_id, chat_id, content, sender, create_time
         - Decode content (JSON string -> extract text)
         - Handle Chinese content encoding (FEIS-05)
       - `handle_wiki_doc_event(event: dict) -> ExtractedMessage`:
         - Extract doc_token, title
         - Fetch full doc content via API (FEIS-04)

    6. Create `src/saw/api/feishu_webhook.py`:
       - `POST /api/v1/webhooks/feishu`:
         - Parse JSON body
         - Verify signature (lark-oapi handles this)
         - Route to FeishuEventHandler
         - Return quickly

    7. Write unit tests with mock payloads

    Per CONTEXT.md: Use lark-oapi 1.5.5. Ensure UTF-8 encoding for Chinese content.
  </action>
  <verify>
    <automated>pytest tests/unit/test_feishu_connector/test_connector.py -v</automated>
  </verify>
  <done>
    - FeishuConnector implements UnifiedConnectorInterface
    - Message events processed
    - Wiki doc events processed
    - Chinese content handled correctly
    - All connector tests passing (minimum 6 tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create WeCom models and encryption</name>
  <files>
    src/saw/connectors/wecom/__init__.py,
    src/saw/connectors/wecom/models.py,
    src/saw/connectors/wecom/crypto.py,
    src/saw/db/wecom_models.py,
    tests/unit/test_wecom_connector/test_models.py,
    tests/unit/test_wecom_connector/test_crypto.py
  </files>
  <behavior>
    - Test 1: WeComConfig validates webhook_url and encoding_aes_key
    - Test 2: WeComCrypto decrypts AES-256-CBC encrypted messages
    - Test 3: WeComCrypto verifies signature correctly
    - Test 4: PKCS7 padding handled correctly
    - Test 5: Signature verification rejects tampered messages
    - Test 6: Chinese content preserved after decryption
  </behavior>
  <action>
    Create WeCom connector package with encryption support (WECO-01~04):

    1. Create `src/saw/connectors/wecom/__init__.py` with exports

    2. Create `src/saw/connectors/wecom/models.py`:
       - `WeComConfig`: webhook_url, encoding_aes_key, token, corp_id
       - `WeComMessage`: msg_id, from_user, create_time, msg_type, content
       - `WeComEvent`: event_type, event_data

    3. Create `src/saw/connectors/wecom/crypto.py`:

    4. `WeComCrypto`:
       - Constructor: decode encoding_aes_key (base64 + padding)
       - `decrypt(encrypted_msg: str) -> str`:
         - Base64 decode
         - AES-256-CBC decrypt (key=32 bytes, IV=first 16 bytes)
         - Remove PKCS7 padding
         - Parse: random(16) + msg_len(4) + msg + corp_id
         - Return msg as UTF-8 string (WECO-03)
       - `verify_signature(signature: str, timestamp: str, nonce: str, encrypted: str) -> bool`:
         - SHA1(sort(token, timestamp, nonce, encrypted))
         - Timing-safe comparison

    5. Create `src/saw/db/wecom_models.py`:
       - `WeComBotModel`: id, webhook_url, corp_id, is_enabled, created_at
       - Register in `src/saw/db/__init__.py`

    6. Write comprehensive tests for crypto (critical for security)

    Per CONTEXT.md: AES-256-CBC with PKCS7 padding. Use cryptography library.
  </action>
  <verify>
    <automated>pytest tests/unit/test_wecom_connector/test_crypto.py -v</automated>
  </verify>
  <done>
    - WeCom encryption/decryption working
    - Signature verification correct
    - PKCS7 padding handled
    - Chinese content preserved
    - All crypto tests passing (minimum 6 tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Implement WeComConnector and webhook</name>
  <files>
    src/saw/connectors/wecom/connector.py,
    src/saw/api/wecom_webhook.py,
    tests/unit/test_wecom_connector/test_connector.py
  </files>
  <behavior>
    - Test 1: Connector implements UnifiedConnectorInterface
    - Test 2: authenticate() stores webhook config
    - Test 3: Webhook receives and decrypts messages
    - Test 4: Signature verification rejects invalid requests
    - Test 5: transform_to_claim() creates Claim with metadata
  </behavior>
  <action>
    Implement WeComConnector with webhook handling (WECO-01, WECO-02, WECO-04):

    1. Create `src/saw/connectors/wecom/connector.py`:

    2. `WeComConnector(BaseConnector)`:
       - `platform_name = "wecom"`
       - `supports_push = False`
       - Uses httpx for API calls (no official SDK)

    3. `authenticate(config: dict) -> AuthResult`:
       - Store webhook_url, encoding_aes_key, token
       - Initialize WeComCrypto

    4. Create `src/saw/api/wecom_webhook.py`:

    5. `POST /api/v1/webhooks/wecom`:
       - Query params: msg_signature, timestamp, nonce
       - Body: XML with <Encrypt> element
       - Verify signature
       - Decrypt message
       - Parse decrypted XML
       - Route to message handler
       - Return echo string for verification or success

    6. Rate limiting (WECO-04):
       - Use RateLimitManager
       - WeCom limits: ~600 calls/min per app

    7. Write unit tests with mock webhook payloads

    Per CONTEXT.md: No official SDK, use httpx with webhook.
  </action>
  <verify>
    <automated>pytest tests/unit/test_wecom_connector/test_connector.py -v</automated>
  </verify>
  <done>
    - WeComConnector implements UnifiedConnectorInterface
    - Webhook receives and decrypts messages
    - Signature verification working
    - Rate limiting applied
    - All connector tests passing (minimum 5 tests)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Feishu -> SAW webhook | Untrusted input from Feishu events, verify signature |
| WeCom -> SAW webhook | Untrusted input from WeCom, must decrypt and verify signature |
| SAW -> Feishu/WeCom API | Authenticated API calls with tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-14 | Spoofing | wecom/crypto.py | mitigate | Verify SHA1 signature before decryption |
| T-13-15 | Tampering | wecom/crypto.py | mitigate | Signature covers encrypted content; tampering detected |
| T-13-16 | Information Disclosure | feishu/token_manager.py | mitigate | Encrypt tenant tokens with Fernet (AUTH-02) |
| T-13-17 | Information Disclosure | wecom/crypto.py | mitigate | Encoding AES key stored securely, not in logs |
| T-13-18 | DoS | api/feishu_webhook.py | accept | Webhook must respond quickly; process async |
| T-13-19 | Elevation | wecom/connector.py | mitigate | Validate corp_id matches configured before processing |
</threat_model>

<verification>
## Phase-Level Verification

1. Run all Feishu connector tests:
   ```bash
   pytest tests/unit/test_feishu_connector/ -v
   ```

2. Run all WeCom connector tests:
   ```bash
   pytest tests/unit/test_wecom_connector/ -v
   ```

3. Verify encryption/decryption:
   ```bash
   python -c "from saw.connectors.wecom.crypto import WeComCrypto; print('OK')"
   ```

4. Test webhook endpoints:
   ```bash
   python -c "from saw.api.feishu_webhook import router; from saw.api.wecom_webhook import router2; print(len(router.routes), len(router2.routes))"
   ```
</verification>

<success_criteria>
- [ ] User can install Feishu app via OAuth (FEIS-01)
- [ ] System receives messages via webhooks (FEIS-02)
- [ ] Multi-tenant token management working (FEIS-03)
- [ ] Feishu Wiki docs captured (FEIS-04)
- [ ] Chinese content handled correctly (FEIS-05)
- [ ] User can configure WeCom webhook (WECO-01)
- [ ] System receives WeCom messages (WECO-02)
- [ ] AES-256-CBC decryption working (WECO-03)
- [ ] Rate limits respected (WECO-04)
- [ ] All tests passing (minimum 27 tests)
- [ ] Both connectors registered and functional
</success_criteria>

<output>
After completion, create `.planning/phases/13-logseq-im-connectors/13-04-SUMMARY.md`
</output>
