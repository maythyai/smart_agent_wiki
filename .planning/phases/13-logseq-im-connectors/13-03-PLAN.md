---
phase: 13-logseq-im-connectors
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - src/saw/connectors/discord/__init__.py
  - src/saw/connectors/discord/models.py
  - src/saw/connectors/discord/connector.py
  - src/saw/connectors/discord/gateway.py
  - src/saw/db/discord_models.py
  - src/saw/api/discord_webhook.py
  - tests/unit/test_discord_connector/test_models.py
  - tests/unit/test_discord_connector/test_connector.py
  - tests/unit/test_discord_connector/test_gateway.py
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - DISC-05
user_setup:
  - service: discord
    why: "Discord bot for message ingestion via Gateway"
    env_vars:
      - name: DISCORD_BOT_TOKEN
        source: "Discord Developer Portal -> Applications -> Your App -> Bot -> Token"
    dashboard_config:
      - task: "Create Discord Application"
        location: "https://discord.com/developers/applications -> New Application"
      - task: "Create Bot User"
        location: "Discord App -> Bot -> Add Bot"
      - task: "Enable Message Content Intent"
        location: "Discord App -> Bot -> Privileged Gateway Intents -> Message Content Intent"
      - task: "Copy Bot Token"
        location: "Discord App -> Bot -> Token -> Reset Token (copy immediately)"
      - task: "Add Bot to Server"
        location: "Discord App -> OAuth2 -> URL Generator -> scopes: bot -> permissions: Read Messages, Read Message History -> Copy URL and open in browser"
must_haves:
  truths:
    - "User can add Discord bot to server and receive messages via Gateway"
    - "System handles WebSocket reconnection with resume sequence"
    - "System captures embeds and attachments from messages"
    - "System respects Discord's 50 req/sec global rate limit"
    - "Messages are processed through MessageHandler"
  artifacts:
    - path: "src/saw/connectors/discord/connector.py"
      provides: "DiscordConnector implementing UnifiedConnectorInterface"
      exports: ["DiscordConnector"]
    - path: "src/saw/connectors/discord/gateway.py"
      provides: "Discord Gateway WebSocket management with reconnection"
      exports: ["DiscordGateway", "DiscordConnectionState"]
    - path: "src/saw/db/discord_models.py"
      provides: "Discord guild and channel persistence"
      contains: "class DiscordGuildModel"
  key_links:
    - from: "src/saw/connectors/discord/connector.py"
      to: "src/saw/connectors/protocol.py"
      via: "implements UnifiedConnectorInterface"
      pattern: "class DiscordConnector.*Protocol"
    - from: "src/saw/connectors/discord/gateway.py"
      to: "src/saw/connectors/message_handler.py"
      via: "on_message calls MessageHandler.extract_from_discord"
      pattern: "MessageHandler"
---

<objective>
Implement Discord connector with Gateway WebSocket for real-time message ingestion, reconnection handling with resume sequence, and embed/attachment capture.

Purpose: Enable real-time message ingestion from Discord servers with proper reconnection and rich content support.
Output: Complete Discord connector using discord.py library for Gateway management.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-logseq-im-connectors/CONTEXT.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/saw/connectors/protocol.py:
```python
@runtime_checkable
class UnifiedConnectorInterface(Protocol):
    platform_name: str
    supports_push: bool
    async def authenticate(self, config: dict) -> AuthResult: ...
    async def get_items(self, since: datetime | None = None) -> list[ConnectorItem]: ...
    async def put_item(self, item: ConnectorItem) -> ConnectorItem: ...
    async def delete_item(self, item_id: str) -> bool: ...
    def transform_to_claim(self, item: ConnectorItem) -> Claim: ...
    def transform_from_claim(self, claim: Claim) -> ConnectorItem: ...
```

From src/saw/connectors/message_handler.py:
```python
@dataclass
class ExtractedMessage:
    platform: str
    external_id: str
    content: str
    author: MessageAuthor
    context: MessageContext
    timestamp: datetime
    metadata: dict = field(default_factory=dict)
```

From src/saw/connectors/rate_limiter.py:
```python
class RateLimitManager:
    async def acquire(self, platform: str) -> None: ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Discord models and database schema</name>
  <files>
    src/saw/connectors/discord/__init__.py,
    src/saw/connectors/discord/models.py,
    src/saw/db/discord_models.py,
    tests/unit/test_discord_connector/test_models.py
  </files>
  <behavior>
    - Test 1: DiscordGuildModel stores guild_id and guild_name
    - Test 2: DiscordChannelModel stores channel_id and channel_type
    - Test 3: DiscordMessageEvent validates message payload structure
    - Test 4: DiscordEmbed extracts title, description, url, fields
    - Test 5: DiscordAttachment extracts filename, url, size, content_type
    - Test 6: DiscordConnectionState tracks session_id and sequence for resume
  </behavior>
  <action>
    Create Discord connector package with Pydantic models and SQLAlchemy schema (DISC-01~05):

    1. Create `src/saw/connectors/discord/__init__.py` with exports

    2. Create `src/saw/connectors/discord/models.py`:
       - `DiscordConfig`: bot_token, application_id
       - `DiscordGuild`: guild_id, guild_name, icon_url
       - `DiscordChannel`: channel_id, channel_name, channel_type (text, voice, thread)
       - `DiscordMessageEvent`: message_id, author, content, channel_id, guild_id, timestamp, edited_timestamp
       - `DiscordEmbed`: title, description, url, color, fields (list of name/value pairs)
       - `DiscordAttachment`: id, filename, url, size, content_type
       - `DiscordReaction`: emoji, count
       - `DiscordConnectionState`: session_id, sequence_number, last_heartbeat, reconnect_count

    3. Create `src/saw/db/discord_models.py`:
       - `DiscordGuildModel`: id, guild_id (indexed), guild_name, is_enabled, added_at
       - `DiscordChannelModel`: id, guild_id, channel_id (indexed), channel_name, channel_type, is_enabled
       - `DiscordConnectionStateModel`: id, session_id, sequence_number, last_heartbeat, updated_at
       - Register in `src/saw/db/__init__.py`

    4. Write unit tests for all models

    Use Pydantic v2 for validation. Discord uses snowflake IDs (strings).
  </action>
  <verify>
    <automated>pytest tests/unit/test_discord_connector/test_models.py -v</automated>
  </verify>
  <done>
    - Discord message event models defined
    - Embed and attachment models defined
    - SQLAlchemy models for persistence
    - Connection state model for resume support
    - All model tests passing (minimum 6 tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Discord Gateway WebSocket handler</name>
  <files>
    src/saw/connectors/discord/gateway.py,
    tests/unit/test_discord_connector/test_gateway.py
  </files>
  <behavior>
    - Test 1: Gateway connects with required intents (messages, message_content)
    - Test 2: Gateway stores session_id on READY event
    - Test 3: Gateway sends RESUME opcode on reconnection with session_id
    - Test 4: Gateway handles HEARTBEAT_ACK with sequence tracking
    - Test 5: Gateway captures message_create events
    - Test 6: Connection state persisted for resume
  </behavior>
  <action>
    Implement Discord Gateway WebSocket management with reconnection (DISC-02, DISC-03):

    1. Create `src/saw/connectors/discord/gateway.py`:

    2. `DiscordGateway` using `discord.py` library:
       - Use `discord.Client` or `discord.ext.commands.Bot`
       - Required intents: `intents.messages = True`, `intents.message_content = True`
       - Connect with bot token

    3. Event handlers:
       - `on_ready`: Store session_id for resume (DISC-03)
       - `on_message`: Process incoming messages, extract content/attachments/embeds
       - `on_resumed`: Log successful reconnection
       - `on_error`: Handle Gateway errors

    4. Reconnection handling (DISC-03):
       - `discord.py` handles RESUME opcode automatically when session is valid
       - Store sequence_number from Gateway events
       - On reconnect, library sends RESUME with session_id and last sequence
       - If session invalid, library falls back to IDENTIFY (fresh connection)

    5. Connection state persistence:
       - `save_connection_state()`: Store session_id, sequence_number in database
       - `load_connection_state()`: Load for resume attempt
       - Called on READY and periodic heartbeat ACK

    6. Write unit tests with mocked discord.py client

    Per CONTEXT.md: Use discord.py with Intents.messages and Intents.message_content.
  </action>
  <verify>
    <automated>pytest tests/unit/test_discord_connector/test_gateway.py -v</automated>
  </verify>
  <done>
    - Gateway connects with proper intents
    - Session stored for resume
    - Reconnection with RESUME working
    - Message events captured
    - All gateway tests passing (minimum 6 tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement DiscordConnector core</name>
  <files>
    src/saw/connectors/discord/connector.py,
    tests/unit/test_discord_connector/test_connector.py
  </files>
  <behavior>
    - Test 1: Connector implements UnifiedConnectorInterface
    - Test 2: authenticate() validates bot token
    - Test 3: get_items() fetches messages from channel history
    - Test 4: transform_to_claim() creates Claim with embed/attachment metadata
    - Test 5: Rate limiting applied (50 req/sec global)
    - Test 6: Thread context captured for thread messages
    - Test 7: Supports_push is False (read-only for IM)
  </behavior>
  <action>
    Implement DiscordConnector with UnifiedConnectorInterface (DISC-01~05):

    1. Create `src/saw/connectors/discord/connector.py`:

    2. `DiscordConnector(BaseConnector)`:
       - `platform_name = "discord"`
       - `supports_push = False` (read-only ingestion)
       - Uses `discord.py` Client for Gateway and API

    3. `authenticate(config: dict) -> AuthResult`:
       - Validate bot_token is provided
       - Initialize discord.Client with intents
       - Store bot_token for later use

    4. `start_gateway() -> None`:
       - Start Gateway connection in background task
       - Register message handler callback

    5. `get_items(since: datetime | None) -> list[ConnectorItem]`:
       - For historical fetch (not primary use case)
       - Use `channel.history(after=since)` for each enabled channel
       - Apply rate limiting (DISC-05)

    6. `process_message(message: discord.Message) -> ConnectorItem`:
       - Extract: id, content, author, channel, guild, timestamp
       - Extract attachments: filename, url, size, content_type (DISC-04)
       - Extract embeds: title, description, url, fields (DISC-04)
       - Extract thread reference if message is in thread
       - Use MessageHandler for normalization

    7. `transform_to_claim(item: ConnectorItem) -> Claim`:
       - Map to Claim with source_url, confidence, metadata
       - Include embed and attachment references in metadata

    8. Rate limiting integration:
       - `discord.py` has built-in rate limiting (50 req/sec global)
       - Use `RateLimitManager` for additional API calls if needed
       - Built-in handles 429 responses automatically

    9. Write comprehensive unit tests with mocked discord.py

    Per CONTEXT.md: discord.py has built-in rate limiting (50 req/sec global).
  </action>
  <verify>
    <automated>pytest tests/unit/test_discord_connector/test_connector.py -v</automated>
  </verify>
  <done>
    - DiscordConnector implements UnifiedConnectorInterface
    - Bot token authentication working
    - Message processing with embeds/attachments
    - Rate limiting integrated (built-in)
    - All connector tests passing (minimum 7 tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create Discord webhook and API endpoints</name>
  <files>
    src/saw/api/discord_webhook.py,
    tests/unit/test_discord_connector/test_api.py
  </files>
  <behavior>
    - Test 1: POST /api/v1/discord/connect starts Gateway connection
    - Test 2: GET /api/v1/discord/status returns connection state
    - Test 3: POST /api/v1/discord/disconnect stops Gateway
    - Test 4: GET /api/v1/discord/guilds lists connected guilds
  </behavior>
  <action>
    Create FastAPI endpoints for Discord connector management:

    1. Create `src/saw/api/discord_webhook.py`:

    2. Endpoints:
       - `POST /api/v1/discord/connect`: Start Gateway connection with bot_token
       - `GET /api/v1/discord/status`: Return connection state (connected, session_id, guild_count)
       - `POST /api/v1/discord/disconnect`: Stop Gateway gracefully
       - `GET /api/v1/discord/guilds`: List connected guilds with channel counts
       - `POST /api/v1/discord/guilds/{guild_id}/enable`: Enable/disable guild for ingestion
       - `POST /api/v1/discord/guilds/{guild_id}/channels/{channel_id}/enable`: Enable/disable channel

    3. Integration with SyncEngine:
       - Messages from Gateway are enqueued via MessageHandler
       - Manual sync trigger uses get_items() for historical fetch

    4. Write unit tests for API endpoints

    Register router in `src/saw/api/__init__.py`.
  </action>
  <verify>
    <automated>pytest tests/unit/test_discord_connector/test_api.py -v</automated>
  </verify>
  <done>
    - API endpoints for connector management
    - Gateway start/stop via API
    - Status endpoint showing connection state
    - Guild and channel enable/disable
    - All API tests passing (minimum 4 tests)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord Gateway -> SAW | Messages from Discord servers via Gateway, content is untrusted |
| SAW -> Discord API | Authenticated API calls with bot token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | Information Disclosure | connector.py | mitigate | Encrypt bot token with Fernet (AUTH-02) |
| T-13-11 | DoS | gateway.py | accept | Discord message flood possible; rate limiting by discord.py built-in |
| T-13-12 | Spoofing | gateway.py | accept | Discord Gateway uses TLS, message authenticity guaranteed by Discord |
| T-13-13 | Elevation | api/discord_webhook.py | mitigate | Validate bot token format before starting Gateway |
</threat_model>

<verification>
## Phase-Level Verification

1. Run all Discord connector tests:
   ```bash
   pytest tests/unit/test_discord_connector/ -v
   ```

2. Verify connector registration:
   ```bash
   python -c "from saw.connectors.discord.connector import DiscordConnector; print(DiscordConnector.platform_name)"
   ```

3. Test API endpoint mounting:
   ```bash
   python -c "from saw.api.discord_webhook import router; print(len(router.routes))"
   ```
</verification>

<success_criteria>
- [ ] User can add Discord bot to server (DISC-01)
- [ ] System receives messages via Gateway (DISC-02)
- [ ] Reconnection handled with RESUME opcode (DISC-03)
- [ ] Embeds and attachments captured (DISC-04)
- [ ] Rate limits respected via built-in limiter (DISC-05)
- [ ] All tests passing (minimum 23 tests)
- [ ] Connector registered and functional
</success_criteria>

<output>
After completion, create `.planning/phases/13-logseq-im-connectors/13-03-SUMMARY.md`
</output>
