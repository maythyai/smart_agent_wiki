---
phase: 13-logseq-im-connectors
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - src/saw/connectors/slack/__init__.py
  - src/saw/connectors/slack/models.py
  - src/saw/connectors/slack/connector.py
  - src/saw/connectors/slack/events.py
  - src/saw/connectors/slack/oauth.py
  - src/saw/db/slack_models.py
  - src/saw/api/slack_webhook.py
  - tests/unit/test_slack_connector/test_models.py
  - tests/unit/test_slack_connector/test_connector.py
  - tests/unit/test_slack_connector/test_events.py
  - tests/unit/test_slack_connector/test_oauth.py
autonomous: true
requirements:
  - SLAK-01
  - SLAK-02
  - SLAK-03
  - SLAK-04
  - SLAK-05
  - SLAK-06
user_setup:
  - service: slack
    why: "Slack app installation and message ingestion"
    env_vars:
      - name: SLACK_CLIENT_ID
        source: "Slack App configuration page -> OAuth & Permissions"
      - name: SLACK_CLIENT_SECRET
        source: "Slack App configuration page -> OAuth & Permissions"
      - name: SLACK_SIGNING_SECRET
        source: "Slack App configuration page -> Basic Information"
    dashboard_config:
      - task: "Create Slack App"
        location: "https://api.slack.com/apps -> Create New App"
      - task: "Configure OAuth redirect URL"
        location: "Slack App -> OAuth & Permissions -> Add Redirect URL: https://your-domain/api/v1/oauth/slack/callback"
      - task: "Enable Events API"
        location: "Slack App -> Event Subscriptions -> Enable Events -> Request URL: https://your-domain/api/v1/webhooks/slack"
      - task: "Subscribe to message events"
        location: "Slack App -> Event Subscriptions -> Subscribe to bot events: message.channels, message.groups"
      - task: "Install app to workspace"
        location: "Slack App -> Install to Workspace"
must_haves:
  truths:
    - "User can install Slack app and receive message events in real-time"
    - "System verifies Slack webhook signatures with HMAC-SHA256"
    - "System handles message events with thread context"
    - "System captures URL unfurling and attachments"
    - "System respects Slack's tier-based rate limits"
  artifacts:
    - path: "src/saw/connectors/slack/connector.py"
      provides: "SlackConnector implementing UnifiedConnectorInterface"
      exports: ["SlackConnector"]
    - path: "src/saw/connectors/slack/events.py"
      provides: "Slack Events API handler with signature verification"
      exports: ["SlackEventHandler", "verify_slack_signature"]
    - path: "src/saw/connectors/slack/oauth.py"
      provides: "Slack OAuth flow handler"
      exports: ["SlackOAuthHandler"]
    - path: "src/saw/db/slack_models.py"
      provides: "Slack token and sync state persistence"
      contains: "class SlackTeamModel"
  key_links:
    - from: "src/saw/connectors/slack/connector.py"
      to: "src/saw/connectors/protocol.py"
      via: "implements UnifiedConnectorInterface"
      pattern: "class SlackConnector.*Protocol"
    - from: "src/saw/connectors/slack/events.py"
      to: "src/saw/connectors/message_handler.py"
      via: "MessageHandler.extract_from_slack"
      pattern: "MessageHandler"
    - from: "src/saw/api/slack_webhook.py"
      to: "src/saw/connectors/slack/events.py"
      via: "POST endpoint calls handle_event"
      pattern: "handle_event"
---

<objective>
Implement Slack connector with OAuth 2.0 installation, Events API webhooks, message handling with thread context, and Slack-specific rate limiting.

Purpose: Enable real-time message ingestion from Slack workspaces with proper thread context and attachment support.
Output: Complete Slack connector integrated with existing MessageHandler and ReactionProcessor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-logseq-im-connectors/CONTEXT.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/saw/connectors/protocol.py:
```python
@runtime_checkable
class UnifiedConnectorInterface(Protocol):
    platform_name: str
    supports_push: bool
    async def authenticate(self, config: dict) -> AuthResult: ...
    async def get_items(self, since: datetime | None = None) -> list[ConnectorItem]: ...
    async def put_item(self, item: ConnectorItem) -> ConnectorItem: ...
    async def delete_item(self, item_id: str) -> bool: ...
    def transform_to_claim(self, item: ConnectorItem) -> Claim: ...
    def transform_from_claim(self, claim: Claim) -> ConnectorItem: ...
```

From src/saw/connectors/message_handler.py:
```python
@dataclass
class ExtractedMessage:
    platform: str
    external_id: str
    content: str
    author: MessageAuthor
    context: MessageContext
    timestamp: datetime
    edited: bool = False
    deleted: bool = False
    metadata: dict = field(default_factory=dict)
```

From src/saw/connectors/rate_limiter.py:
```python
class RateLimitManager:
    async def acquire(self, platform: str) -> None: ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Slack models and database schema</name>
  <files>
    src/saw/connectors/slack/__init__.py,
    src/saw/connectors/slack/models.py,
    src/saw/db/slack_models.py,
    tests/unit/test_slack_connector/test_models.py
  </files>
  <behavior>
    - Test 1: SlackTeamModel stores team_id and access_token
    - Test 2: SlackMessageEvent validates message payload structure
    - Test 3: SlackThreadInfo extracts thread_ts and parent_user
    - Test 4: SlackAttachment extracts title, text, url fields
    - Test 5: SlackRateLimit tier configuration correct (tier_1 to tier_4)
  </behavior>
  <action>
    Create Slack connector package with Pydantic models and SQLAlchemy schema (SLAK-01~06):

    1. Create `src/saw/connectors/slack/__init__.py` with exports

    2. Create `src/saw/connectors/slack/models.py`:
       - `SlackConfig`: client_id, client_secret, signing_secret, app_id
       - `SlackTeam`: team_id, team_name, bot_user_id, bot_access_token
       - `SlackMessageEvent`: event type, user, text, channel, ts, thread_ts, subtype
       - `SlackThreadInfo`: thread_ts, parent_user_id, reply_count
       - `SlackAttachment`: title, text, url, fallback, image_url
       - `SlackReaction`: name, count, users
       - `SlackRateLimit`: tier-based limits mapping:
         - tier_1: 1 req/min, tier_2: 20 req/min, tier_3: 50 req/min, tier_4: 100 req/min

    3. Create `src/saw/db/slack_models.py`:
       - `SlackTeamModel`: id, team_id (indexed), team_name, bot_user_id, access_token_encrypted, installed_at
       - `SlackChannelModel`: id, team_id, channel_id, channel_name, is_enabled
       - `SlackSyncCursorModel`: id, team_id, last_ts (cursor for pagination)
       - Register in `src/saw/db/__init__.py`

    4. Write unit tests for all models (TDD: write tests first, then implementation)

    Use Pydantic v2 for validation. Handle optional fields properly.
  </action>
  <verify>
    <automated>pytest tests/unit/test_slack_connector/test_models.py -v</automated>
  </verify>
  <done>
    - Slack message event models defined
    - Thread and attachment models defined
    - SQLAlchemy models for persistence registered
    - Rate limit tiers correctly configured
    - All model tests passing (minimum 5 tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Slack OAuth and signature verification</name>
  <files>
    src/saw/connectors/slack/oauth.py,
    tests/unit/test_slack_connector/test_oauth.py
  </files>
  <behavior>
    - Test 1: OAuth URL generated with correct scopes (chat:read, channels:history, groups:history)
    - Test 2: OAuth callback exchanges code for access token
    - Test 3: Signature verification validates HMAC-SHA256 correctly
    - Test 4: Signature verification rejects invalid signatures
    - Test 5: Signature verification rejects timestamps older than 5 minutes
  </behavior>
  <action>
    Implement Slack OAuth flow and webhook signature verification (SLAK-01, SLAK-02):

    1. Create `src/saw/connectors/slack/oauth.py`:

    2. `SlackOAuthHandler`:
       - `get_oauth_config() -> OAuthConfig`: Returns Slack OAuth configuration
       - `get_authorization_url(state: str) -> str`: Build URL with client_id, scope, redirect_uri, state
       - `async exchange_code(code: str) -> SlackTeam`: POST to token_url, parse response

    3. `verify_slack_signature(body: bytes, timestamp: str, signature: str, signing_secret: str) -> bool`:
       - Check timestamp is within 5 minutes (replay attack prevention)
       - Construct sig_basestring: `v0:{timestamp}:{body}`
       - Compute HMAC-SHA256 with signing_secret
       - Compare with signature (format: `v0={hash}`)
       - Use `hmac.compare_digest` for timing-safe comparison

    4. Write unit tests with mock HTTP responses for OAuth

    Per CONTEXT.md: Use slack-bolt for event handling. Verify signatures with signing secret.
  </action>
  <verify>
    <automated>pytest tests/unit/test_slack_connector/test_oauth.py -v</automated>
  </verify>
  <done>
    - OAuth flow generates valid authorization URL
    - Code exchange returns team info
    - Signature verification working with replay protection
    - All OAuth tests passing (minimum 5 tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Slack Events API handler</name>
  <files>
    src/saw/connectors/slack/events.py,
    src/saw/api/slack_webhook.py,
    tests/unit/test_slack_connector/test_events.py
  </files>
  <behavior>
    - Test 1: Event handler processes message.channels events
    - Test 2: Event handler processes message.groups events (private channels)
    - Test 3: Thread context captured with thread_ts as parent_id
    - Test 4: Attachments extracted from message attachment blocks
    - Test 5: URL unfurling metadata captured
    - Test 6: Bot messages and subtypes filtered (bot_message, message_changed, etc.)
    - Test 7: Rate limit headers parsed and applied
  </behavior>
  <action>
    Implement Slack Events API webhook handler (SLAK-02~05):

    1. Create `src/saw/connectors/slack/events.py`:

    2. `SlackEventHandler`:
       - `handle_event(payload: dict) -> list[ExtractedMessage]`: Route by event type
       - `handle_message_event(event: dict, team_id: str) -> ExtractedMessage`: Extract user, text, channel, ts, thread_ts; filter bot messages and subtypes
       - `extract_attachments(message: dict) -> list[SlackAttachment]`: Parse attachment blocks
       - `handle_url_unfurl(message: dict) -> dict`: Extract unfurled URLs

    3. Create `src/saw/api/slack_webhook.py`:
       - `POST /api/v1/webhooks/slack`:
         - Verify signature using `verify_slack_signature`
         - Handle URL verification challenge (respond with challenge string)
         - Route to `SlackEventHandler.handle_event()`
         - Return 200 OK quickly (Slack expects < 3 sec response)
       - Rate limit header parsing from response

    4. Integration with ReactionProcessor for reaction_added events

    Per CONTEXT.md: Use slack-bolt for event handling. Thread context stored as thread_ts.
  </action>
  <verify>
    <automated>pytest tests/unit/test_slack_connector/test_events.py -v</automated>
  </verify>
  <done>
    - Events API endpoint handling message events
    - Thread context captured correctly
    - Attachments and URL unfurls extracted
    - Bot messages filtered
    - URL verification challenge handled
    - All event tests passing (minimum 7 tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement SlackConnector core</name>
  <files>
    src/saw/connectors/slack/connector.py,
    tests/unit/test_slack_connector/test_connector.py
  </files>
  <behavior>
    - Test 1: Connector implements UnifiedConnectorInterface
    - Test 2: authenticate() uses SlackOAuthHandler
    - Test 3: get_items() fetches messages from conversations.history API
    - Test 4: get_items(since) uses oldest parameter for incremental sync
    - Test 5: Rate limiting applied per Slack tier limits
    - Test 6: transform_to_claim() creates Claim with thread context
    - Test 7: Supports_push is False (read-only for IM)
  </behavior>
  <action>
    Implement SlackConnector with UnifiedConnectorInterface (SLAK-01~06):

    1. Create `src/saw/connectors/slack/connector.py`:

    2. `SlackConnector(BaseConnector)`:
       - `platform_name = "slack"`
       - `supports_push = False` (read-only ingestion)
       - Uses `slack-sdk` WebClient for API calls

    3. `authenticate(config: dict) -> AuthResult`: Uses SlackOAuthHandler, stores team info

    4. `get_items(since: datetime | None) -> list[ConnectorItem]`:
       - Use `conversations.history` API for each enabled channel
       - If `since` provided, use `oldest` parameter for incremental fetch
       - Use cursor from `SlackSyncCursorModel` for pagination
       - Apply rate limiting before each API call (SLAK-06)

    5. `get_thread_replies(channel: str, thread_ts: str) -> list[ConnectorItem]`: Use `conversations.replies` API

    6. `transform_to_claim(item: ConnectorItem) -> Claim`: Map to Claim with source_url, confidence, metadata

    7. Rate limiting integration with `RateLimitManager`

    8. Write comprehensive unit tests with mocked Slack API

    Use `slack-sdk` WebClient. Handle pagination with cursor.
  </action>
  <verify>
    <automated>pytest tests/unit/test_slack_connector/test_connector.py -v</automated>
  </verify>
  <done>
    - SlackConnector implements UnifiedConnectorInterface
    - OAuth flow working with team persistence
    - Message fetching with pagination
    - Thread replies captured
    - Rate limiting integrated
    - All connector tests passing (minimum 7 tests)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slack -> SAW webhook | Untrusted input from Slack Events API, must verify signature |
| SAW -> Slack API | Authenticated API calls with user's bot token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Spoofing | slack_webhook.py | mitigate | Verify HMAC-SHA256 signature on all webhook requests |
| T-13-06 | Replay | slack_webhook.py | mitigate | Reject timestamps older than 5 minutes |
| T-13-07 | Information Disclosure | oauth.py | mitigate | Encrypt bot access tokens with Fernet (AUTH-02) |
| T-13-08 | DoS | events.py | accept | Slack requires < 3 sec response; process async after acknowledgment |
| T-13-09 | Elevation | connector.py | mitigate | Validate team_id matches installed workspace before API calls |
</threat_model>

<verification>
## Phase-Level Verification

1. Run all Slack connector tests:
   ```bash
   pytest tests/unit/test_slack_connector/ -v
   ```

2. Verify connector registration:
   ```bash
   python -c "from saw.connectors.slack.connector import SlackConnector; print(SlackConnector.platform_name)"
   ```

3. Test webhook endpoint mounting:
   ```bash
   python -c "from saw.api.slack_webhook import router; print(len(router.routes))"
   ```
</verification>

<success_criteria>
- [ ] User can install Slack app via OAuth (SLAK-01)
- [ ] System receives events via Events API (SLAK-02)
- [ ] Message events handled correctly (SLAK-03)
- [ ] Thread context captured with parent message (SLAK-04)
- [ ] URL unfurling and attachments extracted (SLAK-05)
- [ ] Rate limits respected with tier-based limiting (SLAK-06)
- [ ] All tests passing (minimum 24 tests)
- [ ] Webhook signature verification working
- [ ] Connector registered and functional
</success_criteria>

<output>
After completion, create `.planning/phases/13-logseq-im-connectors/13-02-SUMMARY.md`
</output>
