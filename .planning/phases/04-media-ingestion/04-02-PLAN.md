---
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/saw/ingest/batch_processor.py
  - src/saw/ingest/preview_manager.py
  - src/saw/cli.py
  - tests/unit/ingest/test_batch_processor.py
requirements:
  - MING-07
  - MING-08
autonomous: true
---

# Plan 04-02: Batch Processing & Preview

**Objective:** 实现批量转录处理和转录预览功能

## Context

本计划在 Plan 04-01 的 MediaExtractor 基础上，添加：
- 批量文件处理（并发控制）
- 转录预览机制（用户确认后写入 Claims）

## Tasks

### Task 1: Create Batch Media Processor

<read_first>
- src/saw/ingest/extractors/media.py (MediaExtractor)
- src/saw/config.py (MediaIngestConfig.batch_concurrency)
</read_first>

<action>
创建 `src/saw/ingest/batch_processor.py`：

1. 定义 `BatchMediaProcessor` 类：
   ```python
   class BatchMediaProcessor:
       def __init__(self, config: MediaIngestConfig, engine: IngestionEngine):
           ...
   ```
2. 实现 `process_batch(files: List[str], options: BatchOptions) -> BatchResult` 方法：
   - 使用 asyncio.gather() 控制并发
   - 每个文件处理返回 MediaIngestResult
   - 收集成功/失败结果
3. 实现 `_process_single_file(file_path: str) -> MediaIngestResult` 私有方法
4. 添加进度回调支持：`on_progress: Optional[Callable[[int, int], None]]`
5. 添加取消支持：`cancel()` 方法设置停止标志
</action>

<acceptance_criteria>
- batch_processor.py 存在
- batch_processor.py 包含 `class BatchMediaProcessor`
- batch_processor.py 包含 `async def process_batch()` 方法
- batch_processor.py 使用 `asyncio.gather()` 进行并发控制
- tests 包含 `test_batch_process_multiple_files` 测试函数
- tests 包含 `test_batch_concurrency_limit` 测试函数
</acceptance_criteria>

---

### Task 2: Implement Preview Manager

<read_first>
- src/saw/storage/vault.py (Vault 存储)
- src/saw/ingest/batch_processor.py (批量处理结果)
</read_first>

<action>
创建 `src/saw/ingest/preview_manager.py`：

1. 定义 `PreviewManager` 类：
   ```python
   class PreviewManager:
       def __init__(self, vault: Vault, db_path: str = ":memory:"):
           # 使用内存 SQLite 存储预览结果
           ...
   ```
2. 实现 `save_preview(transcription: TranscriptionResult, metadata: dict) -> str` 方法：
   - 保存到临时 SQLite 表 `previews`
   - 返回 preview_id
3. 实现 `get_preview(preview_id: str) -> PreviewRecord` 方法
4. 实现 `confirm(preview_id: str) -> str` 方法：
   - 迁移预览数据到 Claims 层
   - 删除预览记录
5. 实现 `discard(preview_id: str) -> None` 方法：
   - 删除预览记录
6. 实现 `list_previews() -> List[PreviewSummary]` 方法
</action>

<acceptance_criteria>
- preview_manager.py 存在
- preview_manager.py 包含 `class PreviewManager`
- preview_manager.py 包含 `save_preview()` 方法，返回 preview_id
- preview_manager.py 包含 `confirm()` 方法，迁移数据到 Claims
- preview_manager.py 包含 `discard()` 方法，删除预览记录
- tests 包含 `test_preview_save_and_confirm` 测试函数
- tests 包含 `test_preview_save_and_discard` 测试函数
</acceptance_criteria>

---

### Task 3: Add CLI Commands for Batch & Preview

<read_first>
- src/saw/cli.py (现有 CLI 结构)
- src/saw/ingest/batch_processor.py
- src/saw/ingest/preview_manager.py
</read_first>

<action>
扩展 CLI 支持批量处理和预览：

1. 添加 `ingest-media` 命令组：
   ```python
   @app.command("ingest-media")
   def ingest_media(
       files: List[Path],
       batch: bool = False,
       preview: bool = True,
       model: str = "base",
       ...
   ):
       ...
   ```
2. 实现批量处理模式：
   - `--batch` 启用批量处理
   - `--batch-size N` 控制并发数
3. 实现预览模式：
   - 默认 `--preview` 启用
   - 转录后显示预览，等待用户确认
   - `--no-preview` 跳过预览直接写入
4. 添加预览管理命令：
   - `saw preview list` 列出待确认预览
   - `saw preview confirm <id>` 确认预览
   - `saw preview discard <id>` 丢弃预览
</action>

<acceptance_criteria>
- cli.py 包含 `ingest-media` 命令（或命令组）
- cli.py 支持 `--batch` 参数
- cli.py 支持 `--preview` / `--no-preview` 参数
- cli.py 包含 `preview list` 子命令
- cli.py 包含 `preview confirm` 子命令
- tests 包含 `test_cli_ingest_media_single` 测试函数
- tests 包含 `test_cli_ingest_media_batch` 测试函数
</acceptance_criteria>

---

### Task 4: Add Progress Reporting

<read_first>
- src/saw/ingest/batch_processor.py
</read_first>

<action>
实现进度报告机制：

1. 在 BatchMediaProcessor 添加进度回调：
   ```python
   def set_progress_callback(self, callback: Callable[[BatchProgress], None]):
       self._progress_callback = callback
   ```
2. 定义 BatchProgress 数据类：
   ```python
   @dataclass
   class BatchProgress:
       total: int
       completed: int
       failed: int
       current_file: str
       elapsed_seconds: float
   ```
3. 实现 CLI 进度显示：
   - 使用 rich.progress 显示进度条
   - 显示当前处理文件名
   - 显示预计剩余时间
4. 支持 Web UI 进度（WebSocket 推送）：
   ```python
   def set_websocket_callback(self, ws_send: Callable[[dict], None]):
       ...
   ```
</action>

<acceptance_criteria>
- batch_processor.py 包含 `BatchProgress` 数据类
- batch_processor.py 包含 `set_progress_callback()` 方法
- cli.py 在批量处理时显示进度条（使用 rich）
- tests 包含 `test_batch_progress_callback` 测试函数
</acceptance_criteria>

---

## Verification Criteria

1. **Batch test**: `saw ingest-media ./videos/ --batch --model base` completes without error
2. **Preview test**: Transcription preview shows text, confirmation writes to Claims
3. **Cancel test**: Long-running batch can be cancelled with Ctrl+C

## must_haves

- BatchMediaProcessor 支持并发处理
- PreviewManager 完整实现
- CLI 命令支持批量模式和预览管理
- 进度显示正确

## truths

- 预览数据存储在内存 SQLite，不污染主数据库
- 并发数默认为 3，可配置
- 转录失败不阻塞其他文件
- Ctrl+C 取消后已完成的文件保留结果