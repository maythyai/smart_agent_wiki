---
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/saw/storage/claims.py
  - src/saw/storage/write_queue.py
  - src/saw/api/routes/ingest.py
  - tests/integration/test_media_pipeline.py
requirements:
  - MING-06
autonomous: true
---

# Plan 04-03: Pipeline Integration

**Objective:** 将媒体转录内容集成到现有的 Claims/Wiki 存储管线

## Context

本计划将媒体转录结果与 v1.1 的存储架构集成：
- 通过 Write Queue 写入 Claims 层
- 扩展 Vault metadata 支持 media_info
- 确保 Claims/Wiki 管线正确处理转录文本

## Tasks

### Task 1: Extend Vault Metadata Schema

<read_first>
- src/saw/storage/vault.py (Vault 存储)
- src/saw/models/vault_entry.py (数据模型)
</read_first>

<action>
扩展 Vault 支持媒体元数据：

1. 在 VaultEntry 模型添加 `media_info: Optional[MediaInfo]` 字段：
   ```python
   @dataclass
   class MediaInfo:
       duration_seconds: float
       format: str
       bitrate_kbps: Optional[int]
       whisper_model: str
       language: str
       transcription_timestamp: datetime
   ```
2. 在 Vault.metadata 字段添加 `media_info` 键支持
3. 更新 Vault 的 `store()` 方法处理 media_info
4. 添加 `get_media_entries()` 查询方法
5. 更新数据库迁移脚本（如使用 Alembic）
</action>

<acceptance_criteria>
- vault_entry.py 包含 `MediaInfo` 数据类
- vault_entry.py 包含 `media_info: Optional[MediaInfo]` 字段
- vault.py 在存储时处理 media_info
- tests 包含 `test_vault_store_media_info` 测试函数
- tests 包含 `test_vault_get_media_entries` 测试函数
</acceptance_criteria>

---

### Task 2: Create Media Claims Writer

<read_first>
- src/saw/storage/claims.py (Claims 存储)
- src/saw/storage/write_queue.py (Write Queue)
- src/saw/ingest/preview_manager.py (PreviewManager)
</read_first>

<action>
实现媒体转录内容写入 Claims 层：

1. 创建 `src/saw/ingest/media_claims_writer.py`：
   ```python
   class MediaClaimsWriter:
       def __init__(self, write_queue: WriteQueue):
           ...
   ```
2. 实现 `write_transcription(preview_id: str, transcription: TranscriptionResult) -> List[str]` 方法：
   - 将转录文本分段为 Claims
   - 每段保留时间戳信息
   - 通过 Write Queue 写入
   - 返回 claim_ids 列表
3. 实现 `_segment_transcription(text: str, segments: List[Segment]) -> List[ClaimInput]` 方法：
   - 按段落/时间戳分割
   - 每段生成一个 ClaimInput
4. 实现 `link_claims_to_vault(claim_ids: List[str], vault_id: str) -> None` 方法：
   - 建立 Claims → Vault 的溯源链接
</action>

<acceptance_criteria>
- media_claims_writer.py 存在
- media_claims_writer.py 包含 `class MediaClaimsWriter`
- media_claims_writer.py 包含 `write_transcription()` 方法
- media_claims_writer.py 包含 `_segment_transcription()` 方法
- write_transcription() 返回 claim_ids 列表
- tests 包含 `test_write_transcription_creates_claims` 测试函数
- tests 包含 `test_segment_transcription_by_timestamp` 测试函数
</acceptance_criteria>

---

### Task 3: Integrate with Write Queue Sinks

<read_first>
- src/saw/storage/write_queue.py
- src/saw/storage/sinks/ (现有 sinks)
</read_first>

<action>
确保 Write Queue 正确处理媒体 Claims：

1. 检查 ClaimsSink 是否需要修改以支持 MediaInfo
2. 确保 IndexSink 正确索引转录文本
3. 确保 AuditSink 记录媒体摄入操作
4. 添加 `MediaIngestOperation` 操作类型到 Write Queue：
   ```python
   class MediaIngestOperation(NamedTuple):
       vault_id: str
       preview_id: str
       transcription: TranscriptionResult
       metadata: dict
   ```
5. 在 Write Queue 添加媒体处理逻辑
</action>

<acceptance_criteria>
- write_queue.py 支持媒体相关操作
- ClaimsSink 正确处理带有 media_info 的 Claims
- IndexSink 正确索引转录文本
- tests 包含 `test_write_queue_media_operation` 测试函数
</acceptance_criteria>

---

### Task 4: Add API Endpoints for Media Ingestion

<read_first>
- src/saw/api/routes/ingest.py (现有 API 路由)
- src/saw/api/server.py (FastAPI 应用)
</read_first>

<action>
扩展 Web API 支持媒体摄入：

1. 添加 `POST /api/v1/ingest/media` 端点：
   - 接受 multipart/form-data 上传文件
   - 支持 `model`, `preview`, `language` 参数
   - 返回 `{"preview_id": "...", "status": "previewing"}`
2. 添加 `POST /api/v1/ingest/media/batch` 端点：
   - 接受多个文件上传
   - 返回批量处理任务 ID
3. 添加 `GET /api/v1/preview/{preview_id}` 端点：
   - 返回预览内容
4. 添加 `POST /api/v1/preview/{preview_id}/confirm` 端点：
   - 确认预览，写入 Claims
5. 添加 `DELETE /api/v1/preview/{preview_id}` 端点：
   - 丢弃预览
6. 添加 WebSocket 进度推送：
   - `/ws/ingest/{task_id}` 进度更新
</action>

<acceptance_criteria>
- ingest.py 包含 `POST /api/v1/ingest/media` 路由
- ingest.py 包含 `POST /api/v1/ingest/media/batch` 路由
- ingest.py 包含 `GET /api/v1/preview/{preview_id}` 路由
- ingest.py 包含 `POST /api/v1/preview/{preview_id}/confirm` 路由
- tests 包含 `test_api_ingest_media` 测试函数
- tests 包含 `test_api_preview_confirm` 测试函数
</acceptance_criteria>

---

### Task 5: End-to-End Integration Test

<read_first>
- tests/integration/ (现有集成测试)
- src/saw/ingest/ (完整摄入管线)
</read_first>

<action>
编写端到端集成测试：

1. 创建 `tests/integration/test_media_pipeline.py`
2. 测试场景 1：单文件完整流程
   - 上传 MP3 → 预览 → 确认 → 验证 Claims 创建
3. 测试场景 2：批量文件处理
   - 上传多个文件 → 验证所有 Claims 创建
4. 测试场景 3：查询验证
   - 摄入后搜索转录内容 → 验证结果
5. 测试场景 4：溯源链验证
   - Claims → Vault → 原始文件链接完整
6. 添加测试音频/视频文件到 `tests/fixtures/media/`
</action>

<acceptance_criteria>
- tests/integration/test_media_pipeline.py 存在
- 包含 `test_single_file_complete_pipeline` 测试函数
- 包含 `test_batch_processing_pipeline` 测试函数
- 包含 `test_search_transcription` 测试函数
- 包含 `test_provenance_chain` 测试函数
- 测试文件运行 `pytest tests/integration/test_media_pipeline.py` exits 0
</acceptance_criteria>

---

## Verification Criteria

1. **E2E test**: Media file uploaded via API, transcribed, preview confirmed, searchable
2. **Provenance**: Every Claim links back to Vault entry which links to original file
3. **Search**: Transcribed content appears in search results
4. **Performance**: 10-minute video processes within 2x real-time

## must_haves

- Vault 支持媒体元数据存储
- MediaClaimsWriter 正确分段写入 Claims
- Write Queue 处理媒体操作
- API 端点完整实现
- 端到端集成测试通过

## truths

- 转录文本按时间戳分段为多个 Claims
- 每个 Claim 保留时间戳信息用于溯源
- 媒体文件原始数据存储在 Vault（可选保留或删除以节省空间）
- 转录语言自动检测，可手动指定
- 索引覆盖转录文本，支持全文搜索