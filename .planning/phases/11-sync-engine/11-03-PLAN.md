---
phase: 11-sync-engine
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - src/saw/connectors/message_handler.py
  - src/saw/connectors/reaction_processor.py
  - src/saw/api/sync.py
  - src/saw/write_queue/sinks/connector_sink.py
  - src/saw/cli/sync_commands.py
autonomous: true
requirements:
  - SYNC-04
  - IM-03
  - IM-04
  - IM-05
  - IM-07
must_haves:
  truths:
    - "User can trigger manual sync for any connector from CLI or Web UI"
    - "System extracts message content, author, timestamp, channel"
    - "System captures thread context for threaded messages"
    - "System handles message reactions as confidence signals"
  artifacts:
    - path: "src/saw/connectors/message_handler.py"
      provides: "IM message extraction and processing"
      exports: ["MessageHandler", "MessageContext"]
    - path: "src/saw/connectors/reaction_processor.py"
      provides: "Message reaction to confidence signal mapping"
      exports: ["ReactionProcessor", "ReactionConfig"]
    - path: "src/saw/api/sync.py"
      provides: "Sync status and manual trigger API endpoints"
      exports: ["router"]
    - path: "src/saw/write_queue/sinks/connector_sink.py"
      provides: "Write Queue sink for connector sync operations"
      exports: ["ConnectorSink"]
    - path: "src/saw/cli/sync_commands.py"
      provides: "CLI commands for sync operations"
      exports: ["sync_group"]
  key_links:
    - from: "src/saw/api/sync.py"
      to: "src/saw/connectors/sync_engine.py"
      via: "Manual sync trigger endpoint"
      pattern: "SyncEngine"
    - from: "src/saw/connectors/message_handler.py"
      to: "src/saw/connectors/sync_engine.py"
      via: "Message ingestion via sync pipeline"
      pattern: "SyncEngine"
    - from: "src/saw/cli/sync_commands.py"
      to: "src/saw/connectors/sync_engine.py"
      via: "CLI sync commands"
      pattern: "SyncEngine"
---

<objective>
Implement IM message handling, reaction processing, sync API endpoints, and CLI commands for manual sync triggers.

Purpose: Enable message ingestion from IM platforms and provide user-facing sync controls.
Output: MessageHandler, ReactionProcessor, sync API, CLI sync commands, ConnectorSink.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-sync-engine/CONTEXT.md
</context>

<interfaces>
<!-- Key types from Plans 11-01, 11-02 that executor needs -->

From src/saw/connectors/sync_engine.py (Plan 11-01):
```python
class SyncEngine:
    async def sync(self, connector_id: str, options: SyncOptions | None = None) -> SyncResult: ...
    async def sync_pull(self, connector: UnifiedConnectorInterface, options: SyncOptions) -> SyncResult: ...
    async def sync_push(self, connector: UnifiedConnectorInterface, options: SyncOptions) -> SyncResult: ...

@dataclass
class SyncOptions:
    direction: SyncDirection = SyncDirection.BIDIRECTIONAL
    mode: SyncMode = SyncMode.INCREMENTAL
    force: bool = False

@dataclass
class SyncResult:
    success: bool
    items_pulled: int = 0
    items_pushed: int = 0
    items_skipped: int = 0
    conflicts: list[ConflictInfo] = field(default_factory=list)
    errors: list[str] = field(default_factory=list)
```

From src/saw/connectors/health_monitor.py (Plan 11-02):
```python
class HealthMonitor:
    async def record_success(self, connector_id: str, platform: str) -> ConnectorHealth: ...
    async def record_failure(self, connector_id: str, platform: str, error: str) -> ConnectorHealth: ...
    async def get_health(self, connector_id: str) -> ConnectorHealth: ...

class HealthStatus(Enum):
    HEALTHY = "healthy"
    DEGRADED = "degraded"
    UNHEALTHY = "unhealthy"
```

From src/saw/connectors/models.py (Phase 10):
```python
@dataclass
class ConnectorItem:
    id: str
    title: str
    content: str
    url: str | None = None
    author: str | None = None
    created_at: datetime
    updated_at: datetime
    metadata: dict[str, Any] = field(default_factory=dict)
```

From src/saw/write_queue/queue.py (existing):
```python
class WriteQueue:
    async def enqueue(self, item: QueuedWrite) -> str: ...
    def depth(self) -> int: ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement MessageHandler for IM ingestion</name>
  <files>src/saw/connectors/message_handler.py, tests/test_message_handler.py</files>
  <behavior>
    - Test 1: MessageHandler.extract_message() creates Claim with content, author, timestamp
    - Test 2: MessageHandler stores thread_parent_id for threaded messages
    - Test 3: MessageHandler extracts channel and server metadata
    - Test 4: MessageHandler handles message edits with version tracking
    - Test 5: MessageHandler handles message deletions gracefully
    - Test 6: MessageHandler normalizes mentions and links
  </behavior>
  <action>
    Create message handler for IM platform ingestion.

    Create `src/saw/connectors/message_handler.py`:

    1. `MessageContext` dataclass:
       - `platform: str` (slack, discord, feishu, wecom)
       - `channel_id: str`
       - `channel_name: str | None`
       - `server_id: str | None` (Discord guild, Slack team)
       - `server_name: str | None`
       - `thread_parent_id: str | None`
       - `thread_root_id: str | None` (for nested threads)

    2. `MessageAuthor` dataclass:
       - `user_id: str`
       - `username: str`
       - `display_name: str | None`
       - `is_bot: bool = False`
       - `avatar_url: str | None`

    3. `ExtractedMessage` dataclass:
       - `platform_id: str`
       - `content: str`
       - `content_raw: str | None` (original markdown/formatting)
       - `author: MessageAuthor`
       - `context: MessageContext`
       - `created_at: datetime`
       - `edited_at: datetime | None`
       - `deleted_at: datetime | None`
       - `reactions: dict[str, int]` (emoji -> count)
       - `attachments: list[str]` (URLs)
       - `mentions: list[str]` (user IDs)
       - `links: list[str]` (extracted URLs)

    4. `MessageHandler` class:
       - `__init__(self, session: AsyncSession)`
       - `def extract_message(self, item: ConnectorItem, platform: str) -> ExtractedMessage`
         - Parse item.metadata for platform-specific fields
         - Normalize content (remove platform-specific formatting)
         - Extract thread context
         - Extract reactions
       - `def to_claim_create(self, message: ExtractedMessage) -> ClaimCreate`
         - Map to ClaimCreate with proper metadata:
           - `source_platform`: platform name
           - `source_id`: platform message ID
           - `source_url`: permalink if available
           - `thread_parent_id`: for threaded messages
           - `channel_id`, `channel_name`: for context
           - `author_id`, `author_name`: for attribution
           - `reactions`: dict for confidence signals
       - `def normalize_content(self, content: str, platform: str) -> str`
         - Slack: remove `<@U123>` user mentions, convert `<!channel>` mentions
         - Discord: convert `<@!123>` mentions, preserve markdown
         - Feishu: handle rich text formatting
       - `async def handle_edit(self, message: ExtractedMessage, existing_claim_id: str) -> Claim`
         - Create new version with edit history
       - `async def handle_deletion(self, message: ExtractedMessage) -> None`
         - Mark Claim as deleted or remove from sync

    Per IM-03: Extract message content, author, timestamp, channel.
    Per IM-04: Capture thread context with thread_parent_id.
  </action>
  <verify>
    <automated>pytest tests/test_message_handler.py -v</automated>
  </verify>
  <done>
    - MessageHandler extracts all required message fields
    - Thread context stored in metadata
    - All 6 tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ReactionProcessor for confidence signals</name>
  <files>src/saw/connectors/reaction_processor.py, tests/test_reaction_processor.py</files>
  <behavior>
    - Test 1: ReactionProcessor maps thumbs up to positive confidence boost
    - Test 2: ReactionProcessor maps thumbs down to negative confidence
    - Test 3: ReactionProcessor calculates weighted confidence from multiple reactions
    - Test 4: ReactionProcessor handles custom emoji reactions
    - Test 5: ReactionProcessor ignores bot reactions
  </behavior>
  <action>
    Create reaction processor for confidence signal mapping.

    Create `src/saw/connectors/reaction_processor.py`:

    1. `ReactionConfig` dataclass:
       - `positive_emojis: dict[str, float]` (emoji -> weight)
         - Default: `{"👍": 1.0, "👍🏻": 1.0, "+1": 1.0, "✅": 0.8, "❤️": 0.5}`
       - `negative_emojis: dict[str, float]`
         - Default: `{"👎": -1.0, "👎🏻": -1.0, "-1": -1.0, "❌": -0.5}`
       - `neutral_emojis: set[str]`
         - Default: `{"👀", "🤔", "💭"}` (no confidence impact)
       - `min_reaction_count: int = 1` (minimum to affect confidence)
       - `max_confidence_delta: float = 0.3` (cap on reaction-based adjustment)

    2. `ReactionResult` dataclass:
       - `confidence_delta: float` (-1.0 to 1.0)
       - `positive_count: int`
       - `negative_count: int`
       - `neutral_count: int`
       - `weighted_score: float`
       - `contributors: dict[str, int]` (emoji -> count)

    3. `ReactionProcessor` class:
       - `__init__(self, config: ReactionConfig | None = None)`
       - `def process_reactions(self, reactions: dict[str, int]) -> ReactionResult`
         - Categorize reactions as positive/negative/neutral
         - Calculate weighted score
         - Apply min_reaction_count filter
         - Cap at max_confidence_delta
       - `def get_confidence_adjustment(self, reactions: dict[str, int], base_confidence: float) -> float`
         - Adjust base confidence by reaction delta
         - Clamp to valid confidence range (0.0 to 1.0)
       - `def is_significant(self, reactions: dict[str, int]) -> bool`
         - Returns True if reactions exceed min_reaction_count
       - `def categorize_emoji(self, emoji: str) -> str`
         - Returns "positive", "negative", "neutral", or "unknown"

    4. Integration with Claim confidence:
       - ReactionProcessor result stored in Claim metadata
       - Used as input to confidence calculation in Learn Engine
       - Reaction counts update over time (webhook events)

    Per IM-05: Handle message reactions as confidence signals.
  </action>
  <verify>
    <automated>pytest tests/test_reaction_processor.py -v</automated>
  </verify>
  <done>
    - ReactionProcessor maps reactions to confidence adjustments
    - Positive/negative/neutral emojis categorized
    - All 5 tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement sync API endpoints and CLI commands</name>
  <files>src/saw/api/sync.py, src/saw/cli/sync_commands.py, tests/test_sync_api.py</files>
  <behavior>
    - Test 1: GET /api/v1/sync/status returns all connector statuses
    - Test 2: POST /api/v1/sync/{connector_id}/trigger starts manual sync
    - Test 3: GET /api/v1/sync/{connector_id}/status returns specific connector
    - Test 4: CLI `saw sync status` shows formatted status table
    - Test 5: CLI `saw sync trigger <connector>` executes sync
    - Test 6: API returns 404 for unknown connector
    - Test 7: API handles sync in-progress gracefully
  </behavior>
  <action>
    Create sync API endpoints and CLI commands for manual sync control.

    1. Create `src/saw/api/sync.py`:
       - Pydantic models:
         - `SyncStatusResponse(BaseModel)`:
           - `connector_id: str`
           - `platform: str`
           - `state: str`
           - `last_sync_at: datetime | None`
           - `last_success_at: datetime | None`
           - `last_error: str | None`
           - `items_synced: int`
           - `health_status: str`
         - `SyncTriggerRequest(BaseModel)`:
           - `direction: str = "bidirectional"` (pull, push, bidirectional)
           - `force: bool = False`
         - `SyncTriggerResponse(BaseModel)`:
           - `success: bool`
           - `result: SyncResult | None`
           - `message: str`

       - Endpoints:
         - `GET /api/v1/sync/status` - List all connector sync statuses
         - `GET /api/v1/sync/{connector_id}/status` - Single connector status
         - `POST /api/v1/sync/{connector_id}/trigger` - Manual sync trigger
         - `POST /api/v1/sync/trigger-all` - Sync all connectors (fire and forget)
         - `GET /api/v1/sync/logs` - Recent sync logs (with pagination)

       - Integration:
         - Use SyncEngine for sync operations
         - Use SyncStatusTracker for status
         - Use HealthMonitor for health status
         - Use SyncLogger for logs

    2. Create `src/saw/cli/sync_commands.py`:
       - Typer CLI group for sync commands
       - Commands:
         - `saw sync status [--connector ID]`
           - Display formatted table of sync statuses
           - Show health, last sync, error count
         - `saw sync trigger <connector_id> [--direction pull|push|both] [--force]`
           - Trigger manual sync
           - Show progress and result
         - `saw sync logs [--connector ID] [--limit N]`
           - Show recent sync logs
           - Filter by connector
         - `saw sync health`
           - Show health status for all connectors

       - Output formatting:
         - Use `rich` library for tables
         - Color-coded status (green=healthy, yellow=degraded, red=unhealthy)

    3. Register API router in `src/saw/api/__init__.py`.
    4. Register CLI group in `src/saw/cli/__init__.py`.

    Per SYNC-01: Unified sync status dashboard (API foundation).
    Per SYNC-04: Manual sync trigger per connector from CLI or Web UI.
  </action>
  <verify>
    <automated>pytest tests/test_sync_api.py -v</automated>
  </verify>
  <done>
    - Sync API endpoints return status and accept triggers
    - CLI commands provide sync control
    - All 7 tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement ConnectorSink for Write Queue integration</name>
  <files>src/saw/write_queue/sinks/connector_sink.py, tests/test_connector_sink.py</files>
  <behavior>
    - Test 1: ConnectorSink processes Claim write operations
    - Test 2: ConnectorSink calls SyncEngine.sync_push for outbound writes
    - Test 3: ConnectorSink handles backpressure correctly
    - Test 4: ConnectorSink retries failed pushes
    - Test 5: ConnectorSink logs all operations
  </behavior>
  <action>
    Create Write Queue sink for connector sync operations.

    Create `src/saw/write_queue/sinks/connector_sink.py`:

    1. `ConnectorSinkConfig` dataclass:
       - `enabled_connectors: list[str]` (which connectors to push to)
       - `batch_size: int = 10` (items per push batch)
       - `batch_timeout_seconds: float = 5.0` (max wait before flush)
       - `retry_config: RetryConfig | None = None`

    2. `ConnectorSink` class (inherits from BaseSink):
       - `__init__(self, config: ConnectorSinkConfig, sync_engine: SyncEngine, registry: ConnectorRegistry)`
       - `async def process(self, item: QueuedWrite) -> SinkResult`
         - Check if item should be pushed to any connector
         - Skip if source_platform matches connector (loop prevention)
         - Call sync_engine.sync_push for each enabled connector
       - `async def process_batch(self, items: list[QueuedWrite]) -> list[SinkResult]`
         - Batch multiple items for efficiency
         - Group by target connector
       - `async def is_healthy(self) -> bool`
         - Check if all enabled connectors are healthy
       - `def should_process(self, item: QueuedWrite) -> bool`
         - Returns True if item type is Claim and has syncable content

    3. Integration points:
       - Import BaseSink from `src/saw/write_queue/sinks/base.py`
       - Import QueuedWrite from `src/saw/write_queue/queue.py`
       - Register sink in WriteQueue initialization

    4. Error handling:
       - Use RetryHandler for transient failures
       - Log to SyncLogger
       - Update HealthMonitor on failures

    Per SYNC-05: Write Queue integration for backpressure.
    Per IM-07: Graceful degradation when platforms unavailable.
  </action>
  <verify>
    <automated>pytest tests/test_connector_sink.py -v</automated>
  </verify>
  <done>
    - ConnectorSink processes Claim writes
    - Backpressure handled correctly
    - All 5 tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MessageHandler → User Content | Untrusted message content from external platforms |
| Sync API → Client | Authenticated API for sync control |
| ConnectorSink → External APIs | Outbound writes to third-party platforms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Spoofing | MessageHandler | mitigate | Validate author identity from platform signatures |
| T-11-09 | Tampering | MessageHandler | mitigate | Sanitize message content before storage |
| T-11-10 | Information Disclosure | Sync API | mitigate | Require authentication for sync endpoints |
| T-11-11 | Denial of Service | Sync API | mitigate | Rate limit sync trigger endpoints |
</threat_model>

<verification>
- All 23 tests pass (6 + 5 + 7 + 5)
- `pytest tests/test_message_handler.py tests/test_reaction_processor.py tests/test_sync_api.py tests/test_connector_sink.py -v` passes
- Sync API endpoints accessible via FastAPI router
- CLI commands registered and functional
- ConnectorSink registered in Write Queue
</verification>

<success_criteria>
- [x] User can trigger manual sync from CLI and API (SYNC-04)
- [x] System extracts message content, author, timestamp, channel (IM-03)
- [x] System captures thread context (IM-04)
- [x] System handles reactions as confidence signals (IM-05)
- [x] System gracefully degrades when platforms unavailable (IM-07)
- [x] ConnectorSink integrates with Write Queue (SYNC-05)
</success_criteria>

<output>
After completion, create `.planning/phases/11-sync-engine/11-03-SUMMARY.md`
</output>
