---
phase: 18-connector-settings
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - web/src/pages/ConnectorSettings.tsx
  - web/src/components/settings/SyncConfigSection.tsx
  - web/src/components/settings/PropertyMappingEditor.tsx
  - web/src/components/settings/OAuthStatusSection.tsx
  - web/src/App.tsx
  - web/src/hooks/useConnectorSettings.ts
autonomous: false
requirements: [CONF-01, CONF-04, CONF-06]

must_haves:
  truths:
    - "User can navigate to /integrations/:platform/settings from IntegrationCard"
    - "Settings page displays current sync configuration"
    - "User can change sync interval via dropdown"
    - "User can select sync direction via radio buttons"
    - "Property mapping editor shows dropdowns for Notion/Logseq field mapping"
    - "OAuth status shows Connected/Expiring/Expired badge"
    - "Re-authorize button triggers OAuth flow on click"
    - "Save button persists changes and shows confirmation"
  artifacts:
    - path: "web/src/pages/ConnectorSettings.tsx"
      provides: "Main settings page component"
      contains: "ConnectorSettings"
    - path: "web/src/components/settings/SyncConfigSection.tsx"
      provides: "Sync interval and direction configuration UI"
      contains: "SyncConfigSection"
    - path: "web/src/components/settings/PropertyMappingEditor.tsx"
      provides: "Property mapping dropdowns for Notion/Logseq"
      contains: "PropertyMappingEditor"
    - path: "web/src/components/settings/OAuthStatusSection.tsx"
      provides: "OAuth status badge and re-authorize button"
      contains: "OAuthStatusSection"
    - path: "web/src/hooks/useConnectorSettings.ts"
      provides: "Settings data fetching and mutation hook"
      contains: "useConnectorSettings"
  key_links:
    - from: "web/src/pages/ConnectorSettings.tsx"
      to: "/api/v1/connectors/{platform}/settings"
      via: "useConnectorSettings hook fetch"
      pattern: "api.get.*settings"
    - from: "web/src/App.tsx"
      to: "web/src/pages/ConnectorSettings.tsx"
      via: "React Router route"
      pattern: "Route path.*integrations.*settings"
---

<objective>
Create Settings UI page with sync configuration, property mapping editor, and OAuth re-authorization.

Purpose: Enable users to configure connectors from dedicated settings pages accessible from the Integration Dashboard.
Output: ConnectorSettings page component, sync config section, property mapping editor, OAuth status section, navigation route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-connector-settings/18-CONTEXT.md
@.planning/phases/18-connector-settings/18-UI-SPEC.md

# Existing Frontend Infrastructure (from Phase 03-03, 16, 17)

From web/src/pages/Integrations.tsx (Phase 15):
- Dashboard entry point with connector cards
- useIntegrations hook pattern
- useIntegrationWebSocket for real-time updates

From web/src/components/integrations/IntegrationCard.tsx (Phase 15):
- Card component with platform display
- Settings button to navigate to settings page

From web/src/App.tsx:
- React Router setup with routes

From web/src/hooks/useIntegrations.ts:
- Zustand/fetch pattern for API data
```

<interfaces>
From 18-01-PLAN (Settings API):
```typescript
// GET /api/v1/connectors/{platform}/settings response
interface SettingsResponse {
  sync_interval: "5min" | "15min" | "1hr" | "6hr" | "manual";
  sync_directions: "inbound_only" | "outbound_only" | "bidirectional";
  property_mappings: Record<string, string>;
  rate_limit_override: number | null;
  updated_at: string;
}
```

From 18-UI-SPEC.md:
```typescript
// Sync Interval Options (D-07)
const SYNC_INTERVALS = ["5min", "15min", "1hr", "6hr", "manual"];

// Sync Direction Options (D-09-D-11)
const SYNC_DIRECTIONS = [
  { value: "bidirectional", label: "Bidirectional", icon: "⟳" },
  { value: "inbound_only", label: "Inbound only", icon: "↓" },
  { value: "outbound_only", label: "Outbound only", icon: "↑" },
];

// OAuth Status Badge states
type OAuthStatus = "connected" | "expiring" | "expired";
```

From web/src/pages/Integrations.tsx:
```typescript
// Existing pattern for page layout
export default function Integrations() {
  const { connectors, loading, error } = useIntegrations();
  return (
    <div className="max-w-6xl mx-auto">
      {/* Header */}
      {/* Content */}
    </div>
  );
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create useConnectorSettings hook</name>
  <files>web/src/hooks/useConnectorSettings.ts</files>
  <action>
Create React hook for fetching and mutating connector settings.

Hook provides:
- settings: current settings state (SettingsResponse)
- loading: boolean
- error: string | null
- updateSettings: async (partial: SettingsUpdateRequest) => Promise<void>
- reauthorize: async () => Promise<void> — triggers OAuth flow
- resetToDefaults: async () => Promise<void>

Implementation:
- Use useEffect to fetch settings on mount via GET /api/v1/connectors/{platform}/settings
- Use useCallback for mutation functions
- updateSettings calls PUT endpoint
- reauthorize calls POST /connectors/{platform}/reauth and redirects to authorize_url
- Handle loading/error states

Pattern: Similar to useIntegrations hook but for single connector settings.
  </action>
  <verify>
    <automated>grep -c "useConnectorSettings" web/src/hooks/useConnectorSettings.ts && grep -c "updateSettings" web/src/hooks/useConnectorSettings.ts</automated>
  </verify>
  <done>Hook exports settings, loading, error, updateSettings, reauthorize functions</done>
</task>

<task type="auto">
  <name>Task 2: Create SyncConfigSection component</name>
  <files>web/src/components/settings/SyncConfigSection.tsx</files>
  <action>
Create sync configuration UI section per UI-SPEC.md 2.1-2.3.

Layout:
- Section heading: "Sync Configuration"
- Sync Interval dropdown: 5min, 15min (default), 1hr, 6hr, Manual only
- Sync Direction radio group with icons:
  - ⟳ Bidirectional (two-way sync)
  - ↓ Inbound only (import to SAW)
  - ↑ Outbound only (export to platform)

Props:
- syncInterval: current value
- syncDirection: current value
- onIntervalChange: (value: string) => void
- onDirectionChange: (value: string) => void
- disabled: boolean (when saving)

Accessibility:
- Fieldset/legend for radio group
- aria-describedby for help text
- Visible labels for all controls

Use TailwindCSS classes from UI-SPEC.md (text-sm, text-base, spacing scale).
  </action>
  <verify>
    <automated>grep -c "SyncConfigSection" web/src/components/settings/SyncConfigSection.tsx && grep -c "fieldset" web/src/components/settings/SyncConfigSection.tsx</automated>
  </verify>
  <done>Component renders dropdown and radio group, handles onChange callbacks, accessible markup</done>
</task>

<task type="auto">
  <name>Task 3: Create PropertyMappingEditor component</name>
  <files>web/src/components/settings/PropertyMappingEditor.tsx</files>
  <action>
Create property mapping editor per UI-SPEC.md 2.4 and D-12-D-14.

Layout:
- Section heading: "Property Mappings"
- For Notion/Logseq connectors, show dropdowns:
  - Title → [Platform Property ▼]
  - Content → [Platform Property ▼]
  - Confidence → [Platform Property ▼]
  - Freshness → [Platform Property ▼]
- "Add Mapping" button for custom mappings

Props:
- platform: string — determines which properties to show
- mappings: Record<string, string> — current mappings
- availableProperties: string[] — from connected database
- onMappingChange: (field: string, property: string) => void
- onAddMapping: () => void
- disabled: boolean

For Notion: Query /api/v1/notion/databases/selected to get available properties.
For Logseq: Use predefined property drawer fields.

Only render for platforms that support property mapping (notion, logseq).
Hide for Slack/Discord/GitHub (no property mapping needed).
  </action>
  <verify>
    <automated>grep -c "PropertyMappingEditor" web/src/components/settings/PropertyMappingEditor.tsx && grep -c "availableProperties" web/src/components/settings/PropertyMappingEditor.tsx</automated>
  </verify>
  <done>Component renders property dropdowns for Notion/Logseq, hidden for other platforms, onChange handlers work</done>
</task>

<task type="auto">
  <name>Task 4: Create OAuthStatusSection component</name>
  <files>web/src/components/settings/OAuthStatusSection.tsx</files>
  <action>
Create OAuth status display and re-authorization button per UI-SPEC.md 2.1, 3.1, 3.3 and D-15-D-17.

Layout:
- Section heading: "OAuth Status"
- Status badge with icon:
  - Connected (Green-500): "Connected (expires in N days)"
  - Expiring Soon (Yellow-500): "Expires in N days"
  - Expired (Red-500): "Token expired — re-authorize"
- Re-authorize button:
  - Connected: "Refresh Token" (optional)
  - Expiring: "Re-authorize"
  - Expired: "Re-authorize Now" (urgent styling)

Props:
- status: "connected" | "expiring" | "expired"
- expiresInDays: number | null
- onReauthorize: () => void
- loading: boolean

Button states per UI-SPEC.md 3.3:
- Default: Blue-600 enabled
- Loading: Spinner + "Re-authorizing..."
- Error: Red-600 "Failed"

Only render for OAuth-based platforms (notion, slack, github, feishu).
Hide for Logseq (local file, no OAuth) and WeCom (webhook-based).
  </action>
  <verify>
    <automated>grep -c "OAuthStatusSection" web/src/components/settings/OAuthStatusSection.tsx && grep -c "onReauthorize" web/src/components/settings/OAuthStatusSection.tsx</automated>
  </verify>
  <done>Component renders status badge, re-authorize button with states, hidden for non-OAuth platforms</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>ConnectorSettings page with all sections, routing, and navigation from IntegrationCard</what-built>
  <how-to-verify>
1. Navigate to Integration Dashboard at http://localhost:5173/integrations
2. Click Settings button on any Notion connector card
3. Verify settings page loads at /integrations/notion/settings
4. Verify Sync Configuration section shows current values
5. Verify Property Mapping editor shows dropdowns for Notion
6. Verify OAuth Status shows Connected badge
7. Change sync interval to "1 hour" and click Save
8. Verify "Saved!" confirmation appears
9. Refresh page — settings persist
10. Click "Re-authorize" button — verify OAuth redirect begins
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Settings Page | User interacts with form controls |
| Settings Page → API | PUT request with user-selected values |
| Reauthorize → OAuth Provider | Redirect to external OAuth authorization URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering | Save button | mitigate | Validate form before PUT, show validation errors inline |
| T-18-06 | Information Disclosure | OAuth redirect | accept | Standard OAuth flow, state parameter prevents CSRF |
| T-18-07 | Spoofing | Reauthorize button | mitigate | Verify state parameter matches on OAuth callback |
</threat_model>

<verification>
1. Manual UI verification via checkpoint task
2. Settings page renders at /integrations/:platform/settings
3. All sections render conditionally based on platform type
4. Save button triggers PUT API call and shows confirmation
5. Settings persist after page refresh
</verification>

<success_criteria>
1. User can access settings page from Integration Dashboard (CONF-01)
2. User can view and edit property mappings for Notion/Logseq (CONF-04)
3. User can re-authorize expired OAuth tokens from settings page (CONF-06)
4. Settings page matches UI-SPEC.md layout
5. All sections conditionally render based on platform capabilities
</success_criteria>

<output>
After completion, create `.planning/phases/18-connector-settings/18-02-SUMMARY.md`
</output>