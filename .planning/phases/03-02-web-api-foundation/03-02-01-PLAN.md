---
phase: 03-02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/saw/drivers/web/__init__.py
  - src/saw/drivers/web/app.py
  - src/saw/drivers/web/websocket.py
  - src/saw/drivers/web/routes/__init__.py
  - src/saw/drivers/web/routes/websocket.py
  - src/saw/drivers/web/middleware/__init__.py
  - src/saw/drivers/web/middleware/cors.py
  - src/saw/drivers/web/middleware/errors.py
  - src/saw/drivers/web/schemas/__init__.py
  - src/saw/drivers/web/schemas/websocket.py
  - tests/unit/drivers/web/__init__.py
  - tests/unit/drivers/web/test_websocket.py
  - tests/unit/drivers/web/test_app_factory.py
autonomous: true
requirements: [WEB-01]
user_setup: []

must_haves:
  truths:
    - "FastAPI server starts via `saw web` command"
    - "WebSocket clients can connect to /ws/{session_id}"
    - "WebSocket broadcasts events to connected clients"
    - "CORS middleware allows localhost:3000 for frontend development"
    - "Errors return RFC 7807 Problem Details format"
  artifacts:
    - path: "src/saw/drivers/web/app.py"
      provides: "Application factory with dependency injection"
      exports: ["create_app"]
    - path: "src/saw/drivers/web/websocket.py"
      provides: "WebSocket connection manager"
      exports: ["ConnectionManager", "manager"]
    - path: "src/saw/drivers/web/middleware/errors.py"
      provides: "RFC 7807 error handlers"
      exports: ["register_exception_handlers"]
  key_links:
    - from: "src/saw/drivers/web/app.py"
      to: "QueryEngine, CollaborateEngine, WriteQueue"
      via: "app.state dependency injection"
      pattern: "app\\.state\\.(query|collaborate|write_queue)"
    - from: "src/saw/drivers/web/websocket.py"
      to: "domain events"
      via: "Event Bus subscription"
      pattern: "event_bus\\.subscribe"
---

<objective>
实现 FastAPI Web API 基础架构：Application Factory、WebSocket ConnectionManager、CORS 中间件、RFC 7807 错误处理。

Purpose: 为 Phase 03-03 的 React 前端提供后端 API 基础，支持 RESTful API 和 WebSocket 实时通信。
Output: 可启动的 FastAPI 服务器，WebSocket 连接管理，统一错误处理。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-02-web-api-foundation/03-02-CONTEXT.md
@.planning/phases/03-02-web-api-foundation/03-02-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From src/saw/engines/query/engine.py:
```python
class QueryEngine:
    def query(self, question: str, depth: int = 3, mode: str = "auto", token_budget: int = 4000) -> QueryResult
    def _search: FTS5Search
    def _graph: GraphTraverse
    def _compiler: ContextCompiler

@dataclass
class QueryResult:
    answer: str
    layered_answer: dict[str, str]
    sources: list[dict[str, Any]]
    related_pages: list[str]
    coverage: float
    mode: str
    meta: dict[str, Any]
```

From src/saw/engines/collaborate/orchestrator.py:
```python
class CollaborateEngine:
    async def dispatch_agent(self, agent_name: str, task: AgentTask, context: AgentContext, tools: list[Any] | None = None, check_policy: bool = True) -> AgentResult
    async def execute_workflow(self, workflow_path: Path, inputs: dict[str, Any]) -> WorkflowResult
    def check_policy(self, agent: str, action: str, resource: str = "wiki", context: dict[str, Any] | None = None) -> PolicyDecision
    def get_available_agents(self) -> list[str]
```

From src/saw/write_queue/queue.py:
```python
class SQLiteWriteQueue:
    def enqueue(self, ops: list[WriteOp]) -> None
    def enqueue_atomic(self, ops: list[WriteOp]) -> None
    def get_pending(self) -> list[WriteOp]
    def mark_done(self, op_id: str) -> None
    def track_sink(self, op_id: str, sink_name: str, status: str, error: str | None = None) -> None
```

From src/saw/domain/events.py:
```python
@dataclass(frozen=True)
class ContradictionFound:
    claim_a_uuid: str
    claim_b_uuid: str
    contradiction_type: ContradictionType
    resolution: ResolutionStrategy
    affected_pages: list[str]
    timestamp: datetime

@dataclass(frozen=True)
class ClaimsReady:
    claim_ids: list[str]
    session_id: str

@dataclass(frozen=True)
class IngestCompleted:
    source: str
    claim_count: int
    session_id: str
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Application Factory 和 Middleware</name>
  <files>src/saw/drivers/web/__init__.py, src/saw/drivers/web/app.py, src/saw/drivers/web/middleware/__init__.py, src/saw/drivers/web/middleware/cors.py, src/saw/drivers/web/middleware/errors.py, tests/unit/drivers/web/__init__.py, tests/unit/drivers/web/test_app_factory.py</files>
  
  <read_first>
    - src/saw/drivers/web/__init__.py (不存在则创建)
    - src/saw/engines/query/engine.py (了解 QueryEngine 接口)
    - src/saw/engines/collaborate/orchestrator.py (了解 CollaborateEngine 接口)
    - src/saw/write_queue/queue.py (了解 WriteQueue 接口)
    - .planning/phases/03-02-web-api-foundation/03-02-RESEARCH.md (Pattern 1, Pattern 3 参考)
  </read_first>
  
  <behavior>
    - Test 1: create_app() 返回 FastAPI 实例且包含 CORS 中间件
    - Test 2: lifespan 启动和关闭时 WebSocket broadcaster 正确管理
    - Test 3: 错误处理器返回 RFC 7807 格式 (type, title, status, detail)
  </behavior>
  
  <action>
创建 FastAPI Application Factory 和中间件。

**1. 创建目录结构:**
```
src/saw/drivers/web/
├── __init__.py
├── app.py
├── websocket.py  (Task 2)
├── routes/
│   └── __init__.py
├── middleware/
│   ├── __init__.py
│   ├── cors.py
│   └── errors.py
└── schemas/
    └── __init__.py
```

**2. 实现 app.py - Application Factory (per D-01, D-02, D-03):**
```python
# src/saw/drivers/web/app.py
from typing import TYPE_CHECKING
from contextlib import asynccontextmanager

from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

if TYPE_CHECKING:
    from saw.engines.query.engine import QueryEngine
    from saw.engines.collaborate.orchestrator import CollaborateEngine
    from saw.write_queue.queue import SQLiteWriteQueue

@asynccontextmanager
async def lifespan(app: FastAPI):
    """Manage app lifecycle: startup/shutdown."""
    from .websocket import manager
    # Startup: 启动 WebSocket 广播任务
    if hasattr(app.state, 'event_bus') and app.state.event_bus:
        manager.set_event_bus(app.state.event_bus)
        await manager.start_broadcaster()
    yield
    # Shutdown: 清理资源
    await manager.stop_broadcaster()

def create_app(
    query: "QueryEngine",
    collaborate: "CollaborateEngine",
    write_queue: "SQLiteWriteQueue",
    event_bus = None,
    cors_origins: list[str] | None = None,
    host: str = "127.0.0.1",
    port: int = 8000,
) -> FastAPI:
    """Create FastAPI application with injected dependencies.
    
    Per D-01: FastAPI server with RESTful API and WebSocket support.
    Per D-02: Default port 8000.
    Per D-03: CORS configuration for frontend development.
    """
    app = FastAPI(
        title="Smart Agent Wiki",
        version="1.1.0",
        description="Web API for knowledge management",
        lifespan=lifespan,
    )
    
    # Store engines in app.state for dependency injection
    app.state.query = query
    app.state.collaborate = collaborate
    app.state.write_queue = write_queue
    app.state.event_bus = event_bus
    app.state.host = host
    app.state.port = port
    
    # CORS 配置 (per D-03)
    origins = cors_origins or ["http://localhost:3000", "http://127.0.0.1:3000"]
    app.add_middleware(
        CORSMiddleware,
        allow_origins=origins,
        allow_credentials=True,
        allow_methods=["*"],
        allow_headers=["*"],
    )
    
    # Register error handlers
    from .middleware.errors import register_exception_handlers
    register_exception_handlers(app)
    
    # Register routes (added in later tasks)
    # from .routes import search, graph, pages, websocket
    # app.include_router(...)
    
    return app
```

**3. 实现 middleware/cors.py (per D-03):**
```python
# src/saw/drivers/web/middleware/cors.py
"""CORS configuration for FastAPI."""
from typing import List

DEFAULT_CORS_ORIGINS = [
    "http://localhost:3000",
    "http://127.0.0.1:3000",
]

def get_cors_origins(origins: List[str] | None = None) -> List[str]:
    """Get CORS origins with defaults."""
    return origins or DEFAULT_CORS_ORIGINS
```

**4. 实现 middleware/errors.py (per RESEARCH.md Pattern 5):**
```python
# src/saw/drivers/web/middleware/errors.py
"""RFC 7807 Problem Details error handlers."""
from typing import Any
from fastapi import FastAPI, Request, status
from fastapi.responses import JSONResponse
from fastapi.exceptions import RequestValidationError
from saw.domain.exceptions import SawError

def register_exception_handlers(app: FastAPI) -> None:
    """Register global exception handlers for consistent error responses."""
    
    @app.exception_handler(SawError)
    async def saw_error_handler(request: Request, exc: SawError) -> JSONResponse:
        return JSONResponse(
            status_code=status.HTTP_400_BAD_REQUEST,
            content={
                "type": "https://smart-agent.wiki/errors/business",
                "title": exc.__class__.__name__,
                "status": 400,
                "detail": str(exc),
            },
        )
    
    @app.exception_handler(RequestValidationError)
    async def validation_error_handler(
        request: Request, exc: RequestValidationError
    ) -> JSONResponse:
        return JSONResponse(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            content={
                "type": "https://smart-agent.wiki/errors/validation",
                "title": "Request validation failed",
                "status": 422,
                "detail": exc.errors(),
            },
        )
    
    @app.exception_handler(Exception)
    async def generic_error_handler(request: Request, exc: Exception) -> JSONResponse:
        import logging
        logging.exception("Unhandled exception")
        return JSONResponse(
            status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
            content={
                "type": "https://smart-agent.wiki/errors/internal",
                "title": "Internal server error",
                "status": 500,
                "detail": "An unexpected error occurred",
            },
        )
```

**5. 创建 __init__.py 文件:**
- src/saw/drivers/web/__init__.py: 导出 create_app, manager
- src/saw/drivers/web/middleware/__init__.py: 导出 register_exception_handlers, get_cors_origins
- src/saw/drivers/web/routes/__init__.py: 空文件
- src/saw/drivers/web/schemas/__init__.py: 空文件

**6. 创建测试文件 test_app_factory.py:**
```python
# tests/unit/drivers/web/test_app_factory.py
import pytest
from unittest.mock import MagicMock
from fastapi.testclient import TestClient

from saw.drivers.web.app import create_app

@pytest.fixture
def mock_query_engine():
    engine = MagicMock()
    engine.query.return_value = MagicMock(answer="Mock", sources=[], mode="search")
    return engine

@pytest.fixture
def mock_collaborate_engine():
    return MagicMock()

@pytest.fixture
def mock_write_queue():
    return MagicMock()

@pytest.fixture
def client(mock_query_engine, mock_collaborate_engine, mock_write_queue):
    app = create_app(
        query=mock_query_engine,
        collaborate=mock_collaborate_engine,
        write_queue=mock_write_queue,
    )
    return TestClient(app)

def test_create_app_returns_fastapi_instance(client):
    assert client.app is not None
    assert client.app.title == "Smart Agent Wiki"

def test_create_app_has_cors_middleware(client):
    # Check CORS middleware is registered
    middleware_types = [type(m).__name__ for m in client.app.user_middleware]
    assert "CORSMiddleware" in middleware_types or len(client.app.user_middleware) > 0

def test_error_handler_returns_rfc_7807(client):
    # Trigger validation error
    response = client.get("/api/search?q=")
    assert response.status_code == 422
    data = response.json()
    assert "type" in data or "detail" in data
```
</action>
  
  <verify>
    <automated>pytest tests/unit/drivers/web/test_app_factory.py -x</automated>
  </verify>
  
  <acceptance_criteria>
    - create_app() 函数存在于 src/saw/drivers/web/app.py
    - lifespan 上下文管理器存在于 src/saw/drivers/web/app.py
    - CORS 中间件配置 allow_origins=["http://localhost:3000", "http://127.0.0.1:3000"]
    - register_exception_handlers 函数存在于 src/saw/drivers/web/middleware/errors.py
    - 错误响应包含 type, title, status, detail 字段 (RFC 7807 格式)
  </acceptance_criteria>
  
  <done>Application Factory 可创建 FastAPI 实例，CORS 中间件配置完成，错误处理器返回 RFC 7807 格式。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 WebSocket ConnectionManager 和端点</name>
  <files>src/saw/drivers/web/websocket.py, src/saw/drivers/web/routes/websocket.py, src/saw/drivers/web/schemas/websocket.py, tests/unit/drivers/web/test_websocket.py</files>
  
  <read_first>
    - src/saw/domain/events.py (了解事件类型: ContradictionFound, ClaimsReady, IngestCompleted)
    - .planning/phases/03-02-web-api-foundation/03-02-CONTEXT.md (D-04, D-05, D-06)
    - .planning/phases/03-02-web-api-foundation/03-02-RESEARCH.md (Pattern 2, Pitfall 1, Pitfall 2, Code Examples)
  </read_first>
  
  <behavior>
    - Test 1: ConnectionManager.connect() 添加连接到 session
    - Test 2: ConnectionManager.disconnect() 移除连接
    - Test 3: ConnectionManager.broadcast() 发送消息到所有连接
    - Test 4: WebSocket 消息格式包含 type, payload, timestamp 字段
    - Test 5: 事件映射：AgentProgress -> agent_status, WorkflowStep -> workflow_progress, PageUpdated -> page_updated
  </behavior>
  
  <action>
实现 WebSocket ConnectionManager 和 WebSocket 端点。

**1. 实现 websocket.py - ConnectionManager (per D-04, D-05, D-06):**
```python
# src/saw/drivers/web/websocket.py
import asyncio
import json
from typing import TYPE_CHECKING
from dataclasses import dataclass
from datetime import datetime, timezone

from fastapi import WebSocket

if TYPE_CHECKING:
    from saw.domain.events import ContradictionFound, ClaimsReady, IngestCompleted

@dataclass
class WSMessage:
    """WebSocket message format (per D-05)."""
    type: str
    payload: dict
    timestamp: str
    
    def json(self) -> str:
        return json.dumps({
            "type": self.type,
            "payload": self.payload,
            "timestamp": self.timestamp,
        })

class ConnectionManager:
    """WebSocket connection manager per D-04, D-06."""
    
    def __init__(self):
        # session_id -> set of WebSocket connections
        self._connections: dict[str, set[WebSocket]] = {}
        self._event_bus = None
        self._broadcast_task: asyncio.Task | None = None
    
    def set_event_bus(self, event_bus) -> None:
        """Set event bus for subscription."""
        self._event_bus = event_bus
    
    async def connect(self, websocket: WebSocket, session_id: str) -> None:
        """Accept new connection and add to session group (per D-06)."""
        await websocket.accept()
        if session_id not in self._connections:
            self._connections[session_id] = set()
        self._connections[session_id].add(websocket)
    
    def disconnect(self, websocket: WebSocket, session_id: str) -> None:
        """Remove connection from session group."""
        if session_id in self._connections:
            self._connections[session_id].discard(websocket)
            if not self._connections[session_id]:
                del self._connections[session_id]
    
    async def broadcast(self, session_id: str, message: WSMessage) -> None:
        """Send message to all connections in session (per D-04)."""
        if session_id in self._connections:
            for ws in list(self._connections[session_id]):
                try:
                    await ws.send_text(message.json())
                except Exception:
                    # Connection closed, will be cleaned up
                    pass
    
    async def broadcast_all(self, message: WSMessage) -> None:
        """Broadcast to all sessions."""
        for session_id in list(self._connections.keys()):
            await self.broadcast(session_id, message)
    
    async def start_broadcaster(self) -> None:
        """Start event subscription task."""
        if self._event_bus is None:
            return
        self._broadcast_task = asyncio.create_task(self._broadcast_loop())
    
    async def stop_broadcaster(self) -> None:
        """Stop event subscription task."""
        if self._broadcast_task:
            self._broadcast_task.cancel()
            try:
                await self._broadcast_task
            except asyncio.CancelledError:
                pass
    
    async def _broadcast_loop(self) -> None:
        """Subscribe to events and broadcast to sessions."""
        # Implementation depends on event_bus interface
        # For now, this is a placeholder that can be extended
        try:
            if hasattr(self._event_bus, 'subscribe'):
                async for event in self._event_bus.subscribe():
                    msg = self._event_to_message(event)
                    await self.broadcast_all(msg)
        except asyncio.CancelledError:
            pass
    
    def _event_to_message(self, event) -> WSMessage:
        """Convert domain event to WebSocket message (per D-05)."""
        event_type_map = {
            "AgentProgress": "agent_status",
            "WorkflowStep": "workflow_progress",
            "PageUpdated": "page_updated",
            "ContradictionFound": "page_updated",
            "ClaimsReady": "page_updated",
            "IngestCompleted": "page_updated",
        }
        event_name = type(event).__name__
        return WSMessage(
            type=event_type_map.get(event_name, "unknown"),
            payload=self._event_to_payload(event),
            timestamp=datetime.now(timezone.utc).isoformat(),
        )
    
    def _event_to_payload(self, event) -> dict:
        """Extract payload from event."""
        if hasattr(event, '__dict__'):
            payload = {}
            for k, v in event.__dict__.items():
                # Convert non-serializable types
                if hasattr(v, 'isoformat'):
                    payload[k] = v.isoformat()
                elif hasattr(v, 'name'):
                    payload[k] = v.name
                elif hasattr(v, 'value'):
                    payload[k] = v.value
                else:
                    payload[k] = v
            return payload
        return {}

# Global manager instance
manager = ConnectionManager()
```

**2. 实现 routes/websocket.py (per D-04, D-06):**
```python
# src/saw/drivers/web/routes/websocket.py
import asyncio
from fastapi import APIRouter, WebSocket, WebSocketDisconnect

router = APIRouter()

@router.websocket("/ws/{session_id}")
async def websocket_endpoint(websocket: WebSocket, session_id: str):
    """WebSocket endpoint for real-time updates (per D-04)."""
    from saw.drivers.web.websocket import manager
    
    await manager.connect(websocket, session_id)
    
    try:
        while True:
            data = await websocket.receive_json()
            # Handle client messages (e.g., ping/pong)
            if data.get("type") == "ping":
                await websocket.send_json({"type": "pong", "timestamp": ...})
    except WebSocketDisconnect:
        pass
    except Exception:
        pass
    finally:
        manager.disconnect(websocket, session_id)
```

**3. 实现 schemas/websocket.py:**
```python
# src/saw/drivers/web/schemas/websocket.py
from pydantic import BaseModel
from typing import Any

class WSMessageModel(BaseModel):
    """WebSocket message schema for validation."""
    type: str
    payload: dict[str, Any]
    timestamp: str

class WSPing(BaseModel):
    """Ping message from client."""
    type: str = "ping"

class WSPong(BaseModel):
    """Pong response from server."""
    type: str = "pong"
    timestamp: str
```

**4. 更新 app.py 注册 WebSocket 路由:**
在 create_app() 中添加:
```python
from .routes.websocket import router as ws_router
app.include_router(ws_router, tags=["websocket"])
```

**5. 创建测试文件:**
```python
# tests/unit/drivers/web/test_websocket.py
import pytest
from unittest.mock import AsyncMock, MagicMock
from saw.drivers.web.websocket import ConnectionManager, WSMessage

def test_ws_message_json():
    msg = WSMessage(type="agent_status", payload={"agent": "Librarian"}, timestamp="2024-01-01T00:00:00Z")
    assert msg.json() == '{"type": "agent_status", "payload": {"agent": "Librarian"}, "timestamp": "2024-01-01T00:00:00Z"}'

@pytest.mark.asyncio
async def test_connection_manager_connect():
    manager = ConnectionManager()
    ws = AsyncMock()
    ws.accept = AsyncMock()
    
    await manager.connect(ws, "session-1")
    
    assert "session-1" in manager._connections
    assert ws in manager._connections["session-1"]

@pytest.mark.asyncio
async def test_connection_manager_disconnect():
    manager = ConnectionManager()
    ws = AsyncMock()
    ws.accept = AsyncMock()
    
    await manager.connect(ws, "session-1")
    manager.disconnect(ws, "session-1")
    
    assert "session-1" not in manager._connections

@pytest.mark.asyncio
async def test_connection_manager_broadcast():
    manager = ConnectionManager()
    ws1 = AsyncMock()
    ws1.accept = AsyncMock()
    ws1.send_text = AsyncMock()
    ws2 = AsyncMock()
    ws2.accept = AsyncMock()
    ws2.send_text = AsyncMock()
    
    await manager.connect(ws1, "session-1")
    await manager.connect(ws2, "session-1")
    
    msg = WSMessage(type="test", payload={"key": "value"}, timestamp="2024-01-01T00:00:00Z")
    await manager.broadcast("session-1", msg)
    
    ws1.send_text.assert_called_once_with(msg.json())
    ws2.send_text.assert_called_once_with(msg.json())
```
</action>
  
  <verify>
    <automated>pytest tests/unit/drivers/web/test_websocket.py -x</automated>
  </verify>
  
  <acceptance_criteria>
    - ConnectionManager 类存在于 src/saw/drivers/web/websocket.py
    - manager 全局实例存在于 src/saw/drivers/web/websocket.py
    - connect() 方法调用 websocket.accept()
    - disconnect() 方法从 _connections 中移除连接
    - broadcast() 方法调用 websocket.send_text()
    - WebSocket 端点存在于 /ws/{session_id}
    - 事件类型映射正确：AgentProgress -> agent_status, WorkflowStep -> workflow_progress, PageUpdated -> page_updated
  </acceptance_criteria>
  
  <done>WebSocket ConnectionManager 实现完成，支持连接管理、广播、事件订阅。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → FastAPI | Untrusted HTTP requests from browser |
| WebSocket Client → Server | Untrusted WebSocket messages from browser |
| API → Engine Layer | Trusted internal calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | WebSocket messages | mitigate | JSON schema validation, reject unknown message types |
| T-03-02-02 | Denial | WebSocket connections | mitigate | ConnectionManager cleanup + connection limits per session |
| T-03-02-03 | Spoofing | CORS | mitigate | Explicit origins list, not "*" with credentials |
| T-03-02-04 | Information Disclosure | Error messages | mitigate | RFC 7807 format, no stack traces in production |
</threat_model>

<verification>
## Wave 1 完成验证

- [ ] FastAPI app 可通过 create_app() 创建
- [ ] CORS 中间件配置正确
- [ ] 错误处理返回 RFC 7807 格式
- [ ] WebSocket 可连接到 /ws/{session_id}
- [ ] WebSocket broadcast 功能正常
- [ ] 所有测试通过: `pytest tests/unit/drivers/web/ -x`
</verification>

<success_criteria>
1. FastAPI 服务器可通过 `create_app()` 创建
2. CORS 允许 localhost:3000 前端开发
3. 错误响应遵循 RFC 7807 Problem Details 格式
4. WebSocket 端点 /ws/{session_id} 可接受连接
5. WebSocket 消息格式包含 type, payload, timestamp
6. 所有单元测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/03-02-web-api-foundation/03-02-01-SUMMARY.md`
</output>
