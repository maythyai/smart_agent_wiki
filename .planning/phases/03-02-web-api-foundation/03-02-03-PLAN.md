---
phase: 03-02
plan: 03
type: execute
wave: 2
depends_on: [03-02-01]
files_modified:
  - src/saw/drivers/web/routes/pages.py
  - src/saw/drivers/web/schemas/pages.py
  - src/saw/drivers/cli/commands/web_cmd.py
  - src/saw/drivers/cli/main.py
  - tests/unit/drivers/web/test_pages_api.py
  - tests/integration/test_web_cli.py
autonomous: true
requirements: [WEB-03]
user_setup: []

must_haves:
  truths:
    - "GET /api/pages returns list of all wiki page slugs"
    - "GET /api/pages/{slug} returns page content with frontmatter"
    - "PUT /api/pages/{slug} updates page via Write Queue"
    - "DELETE /api/pages/{slug} deletes page via Write Queue"
    - "CLI command `saw web` starts FastAPI server on port 8000"
    - "CLI supports --host and --port options"
  artifacts:
    - path: "src/saw/drivers/web/routes/pages.py"
      provides: "Page API endpoints"
      exports: ["router"]
    - path: "src/saw/drivers/web/schemas/pages.py"
      provides: "Page request/response schemas"
      exports: ["PageResponse", "PageUpdate", "PageListResponse"]
    - path: "src/saw/drivers/cli/commands/web_cmd.py"
      provides: "CLI web command"
      exports: ["web"]
    - path: "src/saw/drivers/cli/main.py"
      provides: "CLI app registration"
      pattern: "app\\.command.*web"
  key_links:
    - from: "src/saw/drivers/web/routes/pages.py"
      to: "WriteQueue"
      via: "request.app.state.write_queue"
      pattern: "write_queue\\.enqueue"
    - from: "src/saw/drivers/cli/commands/web_cmd.py"
      to: "uvicorn"
      via: "uvicorn.run"
      pattern: "uvicorn\\.run"
---

<objective>
实现 Page API 和 CLI 命令 `saw web`，完成 Web API 基础架构。

Purpose: 为 Phase 03-03 的 React 前端提供 Wiki 页面 CRUD API，并通过 CLI 启动 Web 服务器。
Output: RESTful Page API 端点，CLI 命令启动 FastAPI 服务器。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-02-web-api-foundation/03-02-CONTEXT.md
@.planning/phases/03-02-web-api-foundation/03-02-RESEARCH.md

<interfaces>
<!-- Key types from existing codebase -->

From src/saw/write_queue/queue.py:
```python
@dataclass
class WriteOp:
    op_id: str
    session_id: str
    sink_name: str
    payload: dict
    status: WriteOpStatus
    retry_count: int
    max_retries: int
    error_message: str | None
    created_at: datetime
    updated_at: datetime | None

class SQLiteWriteQueue:
    def enqueue(self, ops: list[WriteOp]) -> None
    def enqueue_atomic(self, ops: list[WriteOp]) -> None
    def get_pending(self) -> list[WriteOp]
    def mark_done(self, op_id: str) -> None
    def track_sink(self, op_id: str, sink_name: str, status: str, error: str | None = None) -> None
```

From src/saw/domain/wiki.py:
```python
@dataclass
class WikiPage:
    path: str
    title: str
    page_type: PageType
    tags: list[str]
    related: list[str]
    confidence: ConfidenceLevel
    freshness: FreshnessLevel
    content: str
    frontmatter: dict[str, Any]
```

From src/saw/drivers/cli/main.py (existing pattern):
```python
app = typer.Typer(name="saw", ...)
# Import and register commands
from saw.drivers.cli.commands.init_cmd import init
app.command(name="init")(init)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 Page API 端点</name>
  <files>src/saw/drivers/web/routes/pages.py, src/saw/drivers/web/schemas/pages.py, tests/unit/drivers/web/test_pages_api.py</files>
  
  <read_first>
    - src/saw/write_queue/queue.py (了解 WriteOp 和 SQLiteWriteQueue 接口)
    - src/saw/domain/wiki.py (了解 WikiPage 类型)
    - src/saw/domain/value_objects.py (了解 WriteOpStatus 枚举)
    - .planning/phases/03-02-web-api-foundation/03-02-CONTEXT.md (D-13, D-14, D-15, D-16)
    - .planning/phases/03-02-web-api-foundation/03-02-RESEARCH.md (Pattern 5)
  </read_first>
  
  <behavior>
    - Test 1: GET /api/pages 返回页面 slug 列表
    - Test 2: GET /api/pages/{slug} 返回页面内容和 frontmatter
    - Test 3: GET /api/pages/nonexistent 返回 404
    - Test 4: PUT /api/pages/{slug} 调用 WriteQueue.enqueue_atomic
    - Test 5: DELETE /api/pages/{slug} 调用 WriteQueue.enqueue_atomic
    - Test 6: PageResponse 包含 slug, title, content, frontmatter, confidence, freshness 字段
  </behavior>
  
  <action>
实现 Page API 端点，所有写入操作通过 Write Queue 持久化。

**1. 实现 schemas/pages.py (per D-13, D-14, D-15, D-16):**
```python
# src/saw/drivers/web/schemas/pages.py
from pydantic import BaseModel, Field
from typing import Any, Optional

class PageResponse(BaseModel):
    """Wiki page content (per D-14)."""
    slug: str
    title: str
    content: str
    frontmatter: dict[str, Any]
    confidence: int = Field(..., ge=1, le=4)
    freshness: int = Field(..., ge=0, le=8)

class PageUpdate(BaseModel):
    """Page update request (per D-15)."""
    content: str
    message: Optional[str] = None  # Commit message

class PageDelete(BaseModel):
    """Page delete request (per D-16)."""
    message: Optional[str] = None  # Deletion reason

class PageListResponse(BaseModel):
    """List of wiki pages."""
    slugs: list[str]
    total: int

class PageCreate(BaseModel):
    """Page creation request."""
    slug: str
    title: str
    content: str
    tags: list[str] = []
    type: str = "summary"

class PageStatus(BaseModel):
    """Write queue status response."""
    status: str
    slug: str
    op_id: Optional[str] = None
```

**2. 实现 routes/pages.py (per D-13, D-14, D-15, D-16):**
```python
# src/saw/drivers/web/routes/pages.py
import uuid
from fastapi import APIRouter, Depends, Request, Path, HTTPException
from typing import Optional

from saw.drivers.web.schemas.pages import (
    PageResponse, PageUpdate, PageDelete, PageListResponse, PageStatus
)

router = APIRouter()

def get_query_engine(request: Request):
    """Dependency: get QueryEngine from app.state."""
    return request.app.state.query

def get_write_queue(request: Request):
    """Dependency: get WriteQueue from app.state."""
    return request.app.state.write_queue

@router.get("/pages", response_model=PageListResponse)
async def list_pages(
    engine = Depends(get_query_engine),
) -> PageListResponse:
    """List all wiki page slugs (per D-13)."""
    # Use WikiRepository to list pages
    pages = engine._wiki_repo.list_pages() if hasattr(engine, '_wiki_repo') else []
    
    return PageListResponse(
        slugs=pages,
        total=len(pages),
    )

@router.get("/pages/{slug}", response_model=PageResponse)
async def get_page(
    slug: str = Path(..., description="Page slug"),
    engine = Depends(get_query_engine),
) -> PageResponse:
    """Get page content (per D-14)."""
    # Read page from WikiRepository
    page = None
    if hasattr(engine, '_wiki_repo'):
        page = engine._wiki_repo.read(slug)
    
    if page is None:
        raise HTTPException(status_code=404, detail=f"Page '{slug}' not found")
    
    # Map confidence and freshness
    confidence_value = page.confidence.value if hasattr(page.confidence, 'value') else 1
    freshness_value = page.freshness.value if hasattr(page.freshness, 'value') else 0
    
    return PageResponse(
        slug=slug,
        title=page.title,
        content=page.content,
        frontmatter=page.frontmatter,
        confidence=confidence_value,
        freshness=freshness_value,
    )

@router.put("/pages/{slug}", response_model=PageStatus)
async def update_page(
    slug: str = Path(..., description="Page slug"),
    update: PageUpdate = ...,
    write_queue = Depends(get_write_queue),
) -> PageStatus:
    """Update page via Write Queue (per D-15).
    
    All mutations flow through Write Queue for durability.
    """
    from saw.write_queue.queue import WriteOp
    from saw.domain.value_objects import WriteOpStatus
    
    op_id = str(uuid.uuid4())
    
    # Create write operations
    ops = [
        WriteOp(
            op_id=op_id,
            session_id="web-api",
            sink_name="wiki",
            payload={
                "op": "write",
                "slug": slug,
                "content": update.content,
                "message": update.message,
            },
            status=WriteOpStatus.PENDING,
        ),
        WriteOp(
            op_id=f"{op_id}-index",
            session_id="web-api",
            sink_name="fts5",
            payload={
                "op": "upsert",
                "slug": slug,
                "content": update.content,
            },
            status=WriteOpStatus.PENDING,
        ),
    ]
    
    # Enqueue atomically
    write_queue.enqueue_atomic(ops)
    
    return PageStatus(
        status="queued",
        slug=slug,
        op_id=op_id,
    )

@router.delete("/pages/{slug}", response_model=PageStatus)
async def delete_page(
    slug: str = Path(..., description="Page slug"),
    delete: PageDelete = None,
    write_queue = Depends(get_write_queue),
) -> PageStatus:
    """Delete page via Write Queue (per D-16)."""
    from saw.write_queue.queue import WriteOp
    from saw.domain.value_objects import WriteOpStatus
    
    op_id = str(uuid.uuid4())
    
    # Create delete operations
    ops = [
        WriteOp(
            op_id=op_id,
            session_id="web-api",
            sink_name="wiki",
            payload={
                "op": "delete",
                "slug": slug,
                "message": delete.message if delete else None,
            },
            status=WriteOpStatus.PENDING,
        ),
        WriteOp(
            op_id=f"{op_id}-index",
            session_id="web-api",
            sink_name="fts5",
            payload={
                "op": "delete",
                "slug": slug,
            },
            status=WriteOpStatus.PENDING,
        ),
    ]
    
    # Enqueue atomically
    write_queue.enqueue_atomic(ops)
    
    return PageStatus(
        status="queued",
        slug=slug,
        op_id=op_id,
    )

@router.post("/pages", response_model=PageStatus)
async def create_page(
    create: PageCreate,
    write_queue = Depends(get_write_queue),
) -> PageStatus:
    """Create new page via Write Queue."""
    from saw.write_queue.queue import WriteOp
    from saw.domain.value_objects import WriteOpStatus
    
    op_id = str(uuid.uuid4())
    
    ops = [
        WriteOp(
            op_id=op_id,
            session_id="web-api",
            sink_name="wiki",
            payload={
                "op": "create",
                "slug": create.slug,
                "title": create.title,
                "content": create.content,
                "tags": create.tags,
                "type": create.type,
            },
            status=WriteOpStatus.PENDING,
        ),
    ]
    
    write_queue.enqueue_atomic(ops)
    
    return PageStatus(
        status="queued",
        slug=create.slug,
        op_id=op_id,
    )
```

**3. 更新 app.py 注册路由:**
在 create_app() 中添加:
```python
from .routes.pages import router as pages_router
app.include_router(pages_router, prefix="/api", tags=["pages"])
```

**4. 创建测试文件:**
```python
# tests/unit/drivers/web/test_pages_api.py
import pytest
from unittest.mock import MagicMock, patch
from fastapi.testclient import TestClient

from saw.drivers.web.app import create_app
from saw.domain.wiki import WikiPage

@pytest.fixture
def mock_wiki_repo():
    repo = MagicMock()
    repo.list_pages.return_value = ["machine-learning", "neural-networks", "transformers"]
    repo.read.return_value = WikiPage(
        path="machine-learning",
        title="Machine Learning",
        content="# Machine Learning\n\nContent here...",
        frontmatter={"type": "summary", "tags": ["ai", "ml"]},
    )
    return repo

@pytest.fixture
def mock_write_queue():
    queue = MagicMock()
    queue.enqueue_atomic = MagicMock()
    return queue

@pytest.fixture
def mock_query_engine(mock_wiki_repo):
    engine = MagicMock()
    engine._wiki_repo = mock_wiki_repo
    return engine

@pytest.fixture
def client(mock_query_engine, mock_write_queue):
    app = create_app(
        query=mock_query_engine,
        collaborate=MagicMock(),
        write_queue=mock_write_queue,
    )
    return TestClient(app)

def test_list_pages(client):
    response = client.get("/api/pages")
    
    assert response.status_code == 200
    data = response.json()
    assert "slugs" in data
    assert "total" in data
    assert data["total"] == 3

def test_get_page(client, mock_wiki_repo):
    response = client.get("/api/pages/machine-learning")
    
    assert response.status_code == 200
    data = response.json()
    assert data["slug"] == "machine-learning"
    assert data["title"] == "Machine Learning"
    assert "content" in data
    assert "frontmatter" in data
    mock_wiki_repo.read.assert_called_once_with("machine-learning")

def test_get_page_not_found(client, mock_wiki_repo):
    mock_wiki_repo.read.return_value = None
    
    response = client.get("/api/pages/nonexistent")
    
    assert response.status_code == 404

def test_update_page(client, mock_write_queue):
    response = client.put(
        "/api/pages/machine-learning",
        json={"content": "Updated content", "message": "Fix typo"},
    )
    
    assert response.status_code == 200
    data = response.json()
    assert data["status"] == "queued"
    assert data["slug"] == "machine-learning"
    mock_write_queue.enqueue_atomic.assert_called_once()

def test_delete_page(client, mock_write_queue):
    response = client.delete(
        "/api/pages/machine-learning",
        json={"message": "Outdated content"},
    )
    
    assert response.status_code == 200
    data = response.json()
    assert data["status"] == "queued"
    mock_write_queue.enqueue_atomic.assert_called_once()

def test_create_page(client, mock_write_queue):
    response = client.post(
        "/api/pages",
        json={
            "slug": "new-page",
            "title": "New Page",
            "content": "# New Page\n\nNew content...",
            "tags": ["new"],
        },
    )
    
    assert response.status_code == 200
    data = response.json()
    assert data["status"] == "queued"
    assert data["slug"] == "new-page"
    mock_write_queue.enqueue_atomic.assert_called_once()

def test_page_response_format(client):
    response = client.get("/api/pages/machine-learning")
    
    assert response.status_code == 200
    data = response.json()
    assert "slug" in data
    assert "title" in data
    assert "content" in data
    assert "frontmatter" in data
    assert "confidence" in data
    assert "freshness" in data
```
</action>
  
  <verify>
    <automated>pytest tests/unit/drivers/web/test_pages_api.py -x</automated>
  </verify>
  
  <acceptance_criteria>
    - GET /api/pages 端点存在于 src/saw/drivers/web/routes/pages.py
    - GET /api/pages/{slug} 端点存在于 src/saw/drivers/web/routes/pages.py
    - PUT /api/pages/{slug} 端点存在于 src/saw/drivers/web/routes/pages.py
    - DELETE /api/pages/{slug} 端点存在于 src/saw/drivers/web/routes/pages.py
    - PageResponse schema 包含 slug, title, content, frontmatter, confidence, freshness 字段
    - PUT 和 DELETE 调用 WriteQueue.enqueue_atomic
    - 页面不存在时返回 404
  </acceptance_criteria>
  
  <done>Page API 端点实现完成，所有写入通过 Write Queue 持久化。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 CLI 命令 `saw web`</name>
  <files>src/saw/drivers/cli/commands/web_cmd.py, src/saw/drivers/cli/main.py, tests/integration/test_web_cli.py</files>
  
  <read_first>
    - src/saw/drivers/cli/main.py (了解 CLI 注册模式)
    - src/saw/drivers/cli/commands/init_cmd.py (参考现有命令实现)
    - src/saw/drivers/web/app.py (了解 create_app 接口)
    - .planning/phases/03-02-web-api-foundation/03-02-CONTEXT.md (D-02)
  </read_first>
  
  <behavior>
    - Test 1: `saw web` 命令注册成功
    - Test 2: `saw web --help` 显示帮助信息
    - Test 3: `saw web --port 9000` 使用指定端口
    - Test 4: `saw web --host 0.0.0.0` 绑定指定主机
    - Test 5: 命令调用 uvicorn.run 启动服务器
  </behavior>
  
  <action>
实现 CLI 命令 `saw web` 启动 FastAPI 服务器。

**1. 实现 commands/web_cmd.py (per D-02):**
```python
# src/saw/drivers/cli/commands/web_cmd.py
"""CLI command to start the web server."""
from __future__ import annotations

import typer
from rich.console import Console
from pathlib import Path

console = Console()

def web(
    host: str = typer.Option(
        "127.0.0.1",
        "--host",
        "-h",
        help="Host to bind the server",
    ),
    port: int = typer.Option(
        8000,
        "--port",
        "-p",
        help="Port to bind the server",
    ),
    reload: bool = typer.Option(
        False,
        "--reload",
        "-r",
        help="Enable auto-reload for development",
    ),
    cors_origins: str = typer.Option(
        "http://localhost:3000,http://127.0.0.1:3000",
        "--cors",
        "-c",
        help="Comma-separated CORS origins",
    ),
) -> None:
    """Start the Smart Agent Wiki web server.
    
    Per D-02: Default port 8000.
    
    Examples:
        saw web                      # Start on http://127.0.0.1:8000
        saw web --port 9000          # Start on port 9000
        saw web --host 0.0.0.0       # Bind to all interfaces
        saw web --reload             # Development mode with auto-reload
    """
    import uvicorn
    from saw.config.loader import Config
    from saw.drivers.web.app import create_app
    
    # Load configuration
    try:
        config = Config.load()
    except Exception as e:
        console.print(f"[yellow]Warning: Could not load config: {e}[/yellow]")
        console.print("[yellow]Using default configuration[/yellow]")
        config = None
    
    # Parse CORS origins
    cors_list = [o.strip() for o in cors_origins.split(",") if o.strip()]
    
    # Build FastAPI app
    # Note: For reload mode, we need to pass the app string instead of app instance
    if reload:
        console.print(f"[blue]Starting development server on http://{host}:{port}[/blue]")
        console.print("[yellow]Auto-reload enabled[/yellow]")
        uvicorn.run(
            "saw.drivers.web.app:create_app_from_config",
            host=host,
            port=port,
            reload=True,
            factory=True,
        )
    else:
        # Get engine instances from config
        from saw.engines.query.engine import QueryEngine
        from saw.engines.collaborate.orchestrator import CollaborateEngine
        from saw.write_queue.queue import SQLiteWriteQueue
        
        # Create minimal instances for server startup
        # Real initialization happens in lifespan
        query_engine = None
        collaborate_engine = None
        write_queue = None
        
        try:
            if config:
                # Initialize from config
                import sqlite3
                db_path = config.db_path if hasattr(config, 'db_path') else ".saw/claims.db"
                conn = sqlite3.connect(str(db_path))
                write_queue = SQLiteWriteQueue(conn)
        except Exception as e:
            console.print(f"[yellow]Warning: Could not initialize engines: {e}[/yellow]")
        
        # Create app
        app = create_app(
            query=query_engine,
            collaborate=collaborate_engine,
            write_queue=write_queue,
            cors_origins=cors_list,
            host=host,
            port=port,
        )
        
        console.print(f"[green]Starting Smart Agent Wiki server[/green]")
        console.print(f"[blue]Server running at http://{host}:{port}[/blue]")
        console.print(f"[blue]API docs at http://{host}:{port}/docs[/blue]")
        console.print("[dim]Press Ctrl+C to stop[/dim]")
        
        uvicorn.run(
            app,
            host=host,
            port=port,
        )
```

**2. 更新 main.py 注册命令:**
```python
# 在 src/saw/drivers/cli/main.py 中添加:
from saw.drivers.cli.commands.web_cmd import web

# 在命令注册部分添加:
app.command(name="web")(web)
```

**3. 创建 helper 函数用于 reload 模式:**
在 app.py 中添加:
```python
# src/saw/drivers/web/app.py 中添加:
def create_app_from_config() -> FastAPI:
    """Factory for uvicorn --reload mode.
    
    Loads configuration and creates app instance.
    """
    from saw.config.loader import Config
    import sqlite3
    from saw.write_queue.queue import SQLiteWriteQueue
    
    try:
        config = Config.load()
        db_path = getattr(config, 'db_path', '.saw/claims.db')
        conn = sqlite3.connect(str(db_path))
        write_queue = SQLiteWriteQueue(conn)
    except Exception:
        # Minimal fallback
        import sqlite3
        conn = sqlite3.connect(":memory:")
        write_queue = SQLiteWriteQueue(conn)
    
    return create_app(
        query=None,
        collaborate=None,
        write_queue=write_queue,
    )
```

**4. 创建集成测试:**
```python
# tests/integration/test_web_cli.py
import pytest
from typer.testing import CliRunner
from unittest.mock import patch, MagicMock

runner = CliRunner()

def test_web_command_help():
    """Test --help shows correct options."""
    from saw.drivers.cli.main import app
    
    result = runner.invoke(app, ["web", "--help"])
    
    assert result.exit_code == 0
    assert "--host" in result.output
    assert "--port" in result.output
    assert "--reload" in result.output
    assert "--cors" in result.output

def test_web_command_default_options():
    """Test web command with default options."""
    from saw.drivers.cli.main import app
    
    with patch("uvicorn.run") as mock_run:
        # Use a short timeout simulation
        result = runner.invoke(app, ["web"], catch_exceptions=False)
        
        # Command should attempt to start
        # Note: Actual server start requires proper DB setup
        # This tests the command registration and parsing only

def test_web_command_custom_port():
    """Test web command with custom port."""
    from saw.drivers.cli.main import app
    
    with patch("uvicorn.run") as mock_run:
        result = runner.invoke(app, ["web", "--port", "9000"], catch_exceptions=False)
        
        # Verify port parameter is parsed correctly

def test_web_command_custom_host():
    """Test web command with custom host."""
    from saw.drivers.cli.main import app
    
    with patch("uvicorn.run") as mock_run:
        result = runner.invoke(app, ["web", "--host", "0.0.0.0"], catch_exceptions=False)

def test_web_command_cors_origins():
    """Test web command with custom CORS origins."""
    from saw.drivers.cli.main import app
    
    with patch("uvicorn.run") as mock_run:
        result = runner.invoke(
            app, 
            ["web", "--cors", "http://localhost:3000,http://example.com"],
            catch_exceptions=False,
        )
```

**5. 创建 conftest.py 共享 fixtures (如果不存在):**
```python
# tests/unit/drivers/web/conftest.py
import pytest
from unittest.mock import MagicMock
from pathlib import Path
import tempfile
import sqlite3

@pytest.fixture
def temp_wiki(tmp_path: Path) -> Path:
    """Create temporary wiki directory."""
    wiki_path = tmp_path / "test-wiki"
    wiki_path.mkdir()
    (wiki_path / ".saw").mkdir()
    (wiki_path / "wiki").mkdir()
    return wiki_path

@pytest.fixture
def mock_query_engine():
    """Mock QueryEngine for testing."""
    engine = MagicMock()
    engine.query.return_value = MagicMock(
        answer="Mock answer",
        sources=[],
        mode="search",
    )
    return engine

@pytest.fixture
def mock_collaborate_engine():
    """Mock CollaborateEngine for testing."""
    engine = MagicMock()
    engine.get_available_agents.return_value = ["Librarian", "Writer", "Critic"]
    return engine

@pytest.fixture
def mock_write_queue():
    """Mock SQLiteWriteQueue for testing."""
    queue = MagicMock()
    queue.enqueue_atomic = MagicMock()
    queue.get_pending.return_value = []
    return queue

@pytest.fixture
def web_app(mock_query_engine, mock_collaborate_engine, mock_write_queue):
    """Create FastAPI app with mocked dependencies."""
    from saw.drivers.web.app import create_app
    return create_app(
        query=mock_query_engine,
        collaborate=mock_collaborate_engine,
        write_queue=mock_write_queue,
    )

@pytest.fixture
def web_client(web_app):
    """Create test client for web app."""
    from fastapi.testclient import TestClient
    return TestClient(web_app)
```
</action>
  
  <verify>
    <automated>pytest tests/integration/test_web_cli.py -x</automated>
  </verify>
  
  <acceptance_criteria>
    - web 函数存在于 src/saw/drivers/cli/commands/web_cmd.py
    - `saw web --help` 显示帮助信息包含 --host, --port, --reload, --cors 选项
    - 命令注册于 src/saw/drivers/cli/main.py
    - 默认端口为 8000 (per D-02)
    - 默认主机为 127.0.0.1
    - --reload 选项启用 uvicorn auto-reload
  </acceptance_criteria>
  
  <done>CLI 命令 `saw web` 实现完成，可通过 Typer CLI 启动 FastAPI 服务器。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Page API | Untrusted page content and slugs |
| CLI → Server | Trusted local execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-09 | Tampering | Page slug path | mitigate | Path validation, reject ".." and absolute paths |
| T-03-02-10 | Tampering | Page content | accept | Content stored as-is, validation by WikiRepository |
| T-03-02-11 | Denial | Large content | mitigate | FastAPI default body limit, Pydantic Field constraints |
| T-03-02-12 | Spoofing | CLI command | accept | Local trusted execution, no remote access |
</threat_model>

<verification>
## Wave 2 完成验证

- [ ] GET /api/pages 返回页面列表
- [ ] GET /api/pages/{slug} 返回页面内容
- [ ] PUT /api/pages/{slug} 通过 Write Queue 更新页面
- [ ] DELETE /api/pages/{slug} 通过 Write Queue 删除页面
- [ ] `saw web` 命令可用
- [ ] `saw web --help` 显示帮助
- [ ] 所有测试通过: `pytest tests/ -v --tb=short`
</verification>

<success_criteria>
1. Page API 提供 GET/PUT/DELETE 端点
2. 所有写入操作通过 Write Queue 持久化
3. CLI 命令 `saw web` 可启动服务器
4. 服务器默认端口 8000
5. 支持 CORS 配置
6. 所有集成测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/03-02-web-api-foundation/03-02-03-SUMMARY.md`
</output>
