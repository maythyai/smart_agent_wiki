---
phase: 03-03-react-frontend
plan: 04
type: execute
wave: 3
depends_on: [03-03-01]
files_modified:
  - web/package.json
  - web/src/types/websocket.ts
  - web/src/stores/editorStore.ts
  - web/src/stores/dashboardStore.ts
  - web/src/stores/index.ts
autonomous: true
requirements: [WEB-03]
user_setup: []

must_haves:
  truths:
    - "WebSocket types are defined and exported"
    - "Editor store slice is registered in combined store"
    - "Dashboard store slice is registered in combined store"
    - "Milkdown dependencies are installed"
  artifacts:
    - path: "web/src/types/websocket.ts"
      provides: "WebSocket message type definitions"
      min_lines: 30
    - path: "web/src/stores/editorStore.ts"
      provides: "Editor state slice"
      modifies: true
    - path: "web/src/stores/dashboardStore.ts"
      provides: "Dashboard state slice"
      min_lines: 30
  key_links:
    - from: "web/src/stores/dashboardStore.ts"
      to: "web/src/types/websocket.ts"
      via: "import"

---

<objective>
Create foundation types and Zustand store slices for Wiki Page Editor and Agent Dashboard features.

Purpose: Establish the type system and state management infrastructure needed by editor and dashboard components.

Output: WebSocket types, editor store slice, dashboard store slice, and Milkdown dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-03-react-frontend/03-03-CONTEXT.md
@.planning/phases/03-03-react-frontend/03-03-RESEARCH.md
</context>

<tasks>

## Task 1: Install Milkdown Dependencies

<read_first>
- web/package.json
</read_first>

<action>
Add Milkdown editor dependencies to web/package.json under "dependencies":
- "@milkdown/react": "7.20.0"
- "@milkdown/kit": "7.20.0"
- "@milkdown/preset-commonmark": "7.20.0"
- "@milkdown/preset-gfm": "7.20.0"
- "@milkdown/plugin-history": "7.20.0"
- "@milkdown/plugin-listener": "7.20.0"

Run `npm install` in web/ directory.
</action>

<verify>
<automated>cd web && npm ls @milkdown/react 2>&1 | grep -q "@milkdown/react"</automated>
</verify>

<done>
`web/package.json` contains Milkdown dependencies and `npm ls @milkdown/react` exits 0.
</done>

---

## Task 2: Create WebSocket Types

<read_first>
- web/src/types/api.ts (for interface pattern reference)
</read_first>

<action>
Create `web/src/types/websocket.ts`:

1. `WSMessage` interface: `type` union ('agent_status' | 'workflow_progress' | 'page_updated'), `data: Record<string, unknown>`, `timestamp: string`

2. `AgentStatus` interface: `agent: string`, `status` union ('idle' | 'running' | 'completed' | 'error'), `task: string | null`, `progress: number`

3. `WorkflowProgress` interface: `workflow_id: string`, `step: string`, `status` union ('pending' | 'running' | 'completed' | 'failed'), `duration_ms: number`

4. `PageUpdatedEvent` interface: `slug: string`, `updated_at: string`

5. `ConnectionStatus` type: 'connecting' | 'connected' | 'disconnected'

Export all interfaces and types.
</action>

<verify>
<automated>cd web && grep -q "export interface WSMessage" src/types/websocket.ts && npx tsc --noEmit</automated>
</verify>

<done>
`web/src/types/websocket.ts` exists with all interfaces.
</done>

---

## Task 3: Update Editor Store Slice

<read_first>
- web/src/stores/index.ts
- web/src/stores/graphStore.ts (slice pattern reference)
</read_first>

<action>
Update `web/src/stores/editorStore.ts` (modify existing slice from 03-03-01):

1. `EditorState` interface:
   - `mode`: `'view' | 'edit' | 'review'` (per D-14)
   - `isDirty`: `boolean`
   - `lastSaved`: `string | null`
   - `content`: `string`

2. `EditorActions` interface:
   - `setMode: (mode) => void`
   - `setContent: (content: string) => void`
   - `markSaved: () => void`
   - `reset: () => void`

3. `createEditorSlice: StateCreator<EditorSlice>` with initial state: `mode: 'view'`, `isDirty: false`, `lastSaved: null`, `content: ''`

4. Export `EditorSlice` type

Verify `web/src/stores/index.ts` imports and spreads `createEditorSlice`.
</action>

<verify>
<automated>cd web && grep -q "createEditorSlice" src/stores/index.ts && npx tsc --noEmit</automated>
</verify>

<done>
`web/src/stores/editorStore.ts` exports `EditorSlice` and `createEditorSlice`.
</done>

---

## Task 4: Create Dashboard Store Slice

<read_first>
- web/src/stores/index.ts
- web/src/types/websocket.ts
</read_first>

<action>
Create `web/src/stores/dashboardStore.ts`:

1. Import `AgentStatus`, `WorkflowProgress`, `ConnectionStatus` from '../types/websocket'

2. `DashboardState` interface:
   - `agents: AgentStatus[]`
   - `workflows: WorkflowProgress[]`
   - `connectionStatus: ConnectionStatus`

3. `DashboardActions` interface:
   - `updateAgent: (agent: AgentStatus) => void`
   - `updateWorkflow: (workflow: WorkflowProgress) => void`
   - `setConnectionStatus: (status: ConnectionStatus) => void`
   - `clearAgents: () => void`

4. `createDashboardSlice: StateCreator<DashboardSlice>` with initial state: `agents: []`, `workflows: []`, `connectionStatus: 'connecting'`

5. Export `DashboardSlice` type

Update `web/src/stores/index.ts` to import and spread `createDashboardSlice`.
</action>

<verify>
<automated>cd web && grep -q "createDashboardSlice" src/stores/index.ts && npx tsc --noEmit</automated>
</verify>

<done>
`web/src/stores/dashboardStore.ts` exports `DashboardSlice` and stores/index.ts imports it.
</done>

</tasks>

<verification>
## Verification Criteria

1. **Build Verification:**
   - `cd web && npm run build` exits 0

2. **Type Verification:**
   - `cd web && npx tsc --noEmit` exits 0

3. **Store Integration:**
   - `web/src/stores/index.ts` imports both `createEditorSlice` and `createDashboardSlice`
</verification>

---

*Phase: 03-03-react-frontend*
*Plan: 04 - Types & Stores Foundation*
*Created: 2026-04-29*
