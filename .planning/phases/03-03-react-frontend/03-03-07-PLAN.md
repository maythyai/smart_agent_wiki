---
phase: 03-03-react-frontend
plan: 07
type: execute
wave: 5
depends_on: [03-03-05]
files_modified:
  - web/src/components/dashboard/AgentCard.tsx
  - web/src/components/dashboard/AgentList.tsx
  - web/src/components/dashboard/ConnectionStatus.tsx
autonomous: true
requirements: [WEB-03]
user_setup: []

must_haves:
  truths:
    - "User can see real-time Agent status cards"
    - "WebSocket connection indicator shows status"
  artifacts:
    - path: "web/src/components/dashboard/AgentList.tsx"
      provides: "Agent status grid"
      min_lines: 25
    - path: "web/src/components/dashboard/ConnectionStatus.tsx"
      provides: "WebSocket status indicator"
      min_lines: 20
  key_links:
    - from: "web/src/components/dashboard/AgentList.tsx"
      to: "web/src/stores/dashboardStore.ts"
      via: "useStore"
---

<objective>
Implement Agent Dashboard UI components for real-time status display.

Purpose: Enable users to monitor agent activity (WEB-03 dashboard).

Output: AgentCard, AgentList, ConnectionStatus components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-03-react-frontend/03-03-CONTEXT.md
</context>

<tasks>

## Task 1: Create AgentCard and AgentList Components

<read_first>
- web/src/types/websocket.ts
- web/src/stores/dashboardStore.ts
</read_first>

<action>
Create `web/src/components/dashboard/AgentCard.tsx` (status badge with color, task, progress bar) and `web/src/components/dashboard/AgentList.tsx` (grid of cards from store, sorted running-first).
</action>

<verify>
<automated>cd web && grep -q "AgentCard" src/components/dashboard/AgentCard.tsx && grep -q "AgentList" src/components/dashboard/AgentList.tsx && npx tsc --noEmit</automated>
</verify>

<done>
AgentCard and AgentList exported.
</done>

---

## Task 2: Create ConnectionStatus Component

<read_first>
- web/src/stores/dashboardStore.ts
- web/src/hooks/useWebSocket.ts
</read_first>

<action>
Create `web/src/components/dashboard/ConnectionStatus.tsx` with colored dot (yellow/green/red) and reconnect button for disconnected state.
</action>

<verify>
<automated>cd web && grep -q "ConnectionStatus" src/components/dashboard/ConnectionStatus.tsx && npx tsc --noEmit</automated>
</verify>

<done>
ConnectionStatus exported.
</done>

</tasks>

<verification>
- `cd web && npm run build` exits 0
</verification>

*Phase: 03-03-react-frontend | Plan: 07 - Dashboard UI*
