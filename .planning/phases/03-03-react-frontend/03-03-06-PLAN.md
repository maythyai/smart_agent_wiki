---
phase: 03-03-react-frontend
plan: 06
type: execute
wave: 5
depends_on: [03-03-05]
files_modified:
  - web/src/components/editor/WikiEditor.tsx
  - web/src/components/editor/EditorToolbar.tsx
  - web/src/components/editor/EditorStatus.tsx
autonomous: true
requirements: [WEB-03]
user_setup: []

must_haves:
  truths:
    - "User can view Wiki page content in Milkdown editor"
    - "User can edit page content with WYSIWYG Markdown"
    - "Editor shows unsaved changes indicator"
  artifacts:
    - path: "web/src/components/editor/WikiEditor.tsx"
      provides: "Milkdown WYSIWYG editor"
      min_lines: 50
      contains: "useEditor"
    - path: "web/src/components/editor/EditorToolbar.tsx"
      provides: "Editor action buttons"
      min_lines: 30
  key_links:
    - from: "web/src/components/editor/WikiEditor.tsx"
      to: "web/src/stores/editorStore.ts"
      via: "useStore"
---

<objective>
Implement Wiki Page Editor UI components with Milkdown WYSIWYG editing.

Purpose: Enable users to view and edit Wiki pages (WEB-03).

Output: WikiEditor, EditorToolbar, EditorStatus components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-03-react-frontend/03-03-CONTEXT.md
</context>

<tasks>

## Task 1: Create WikiEditor Component

<read_first>
- web/src/stores/editorStore.ts
</read_first>

<action>
Create `web/src/components/editor/WikiEditor.tsx` with Milkdown integration using `useEditor`, `commonmark`, `gfm`, `history`, `listener` plugins. Accept `slug`, `initialContent`, `onSave`, `readOnly` props. Sync content via `markdownUpdated` listener.
</action>

<verify>
<automated>cd web && grep -q "useEditor" src/components/editor/WikiEditor.tsx && npx tsc --noEmit</automated>
</verify>

<done>
WikiEditor exports with Milkdown integration.
</done>

---

## Task 2: Create EditorToolbar Component

<read_first>
- web/src/stores/editorStore.ts
</read_first>

<action>
Create `web/src/components/editor/EditorToolbar.tsx` with mode-based buttons (View/Edit/Review), disable Save when not dirty, show spinner when saving.
</action>

<verify>
<automated>cd web && grep -q "mode" src/components/editor/EditorToolbar.tsx && npx tsc --noEmit</automated>
</verify>

<done>
EditorToolbar exports with conditional buttons.
</done>

---

## Task 3: Create EditorStatus Component

<read_first>
- web/src/stores/editorStore.ts
</read_first>

<action>
Create `web/src/components/editor/EditorStatus.tsx` showing "Saving...", "Unsaved changes", or "Saved at {time}" with colored indicators.
</action>

<verify>
<automated>cd web && grep -q "lastSaved" src/components/editor/EditorStatus.tsx && npx tsc --noEmit</automated>
</verify>

<done>
EditorStatus exports with status indicators.
</done>

</tasks>

<verification>
- `cd web && npm run build` exits 0
</verification>

*Phase: 03-03-react-frontend | Plan: 06 - Editor UI*
