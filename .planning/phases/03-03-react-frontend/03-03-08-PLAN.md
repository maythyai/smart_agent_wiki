---
phase: 03-03-react-frontend
plan: 08
type: execute
wave: 6
depends_on: [03-03-06, 03-03-07]
files_modified:
  - web/src/pages/Page.tsx
  - web/src/pages/Dashboard.tsx
  - web/src/routes/router.tsx
  - web/src/__tests__/PageEditor.test.tsx
  - web/src/__tests__/useWebSocket.test.ts
autonomous: true
requirements: [WEB-03]
user_setup: []

must_haves:
  truths:
    - "User can navigate to /page/:slug to view Wiki page"
    - "User can navigate to /dashboard to see Agent status"
  artifacts:
    - path: "web/src/pages/Page.tsx"
      provides: "Wiki page view/edit page"
      min_lines: 40
    - path: "web/src/pages/Dashboard.tsx"
      provides: "Agent dashboard page"
      min_lines: 30
  key_links:
    - from: "web/src/pages/Page.tsx"
      to: "/api/pages/{slug}"
      via: "usePage hook"
---

<objective>
Integrate components into pages, update router, add tests.

Purpose: Complete Web UI for Wiki editing and agent monitoring.

Output: Page.tsx, Dashboard.tsx, router update, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-03-react-frontend/03-03-CONTEXT.md
</context>

<tasks>

## Task 1: Create Page Component

<read_first>
- web/src/components/editor/WikiEditor.tsx
- web/src/hooks/usePage.ts
</read_first>

<action>
Create `web/src/pages/Page.tsx` using `useParams`, `usePage`, editor store. Render title, badges, WikiEditor, toolbar. Handle save/cancel/edit.
</action>

<verify>
<automated>cd web && grep -q "Page" src/pages/Page.tsx && npx tsc --noEmit</automated>
</verify>

<done>
Page.tsx exported.
</done>

---

## Task 2: Create Dashboard Page

<read_first>
- web/src/components/dashboard/AgentList.tsx
- web/src/hooks/useWebSocket.ts
</read_first>

<action>
Create `web/src/pages/Dashboard.tsx` initializing WebSocket, rendering AgentList and ConnectionStatus.
</action>

<verify>
<automated>cd web && grep -q "Dashboard" src/pages/Dashboard.tsx && npx tsc --noEmit</automated>
</verify>

<done>
Dashboard.tsx exported.
</done>

---

## Task 3: Update Router

<read_first>
- web/src/routes/router.tsx
</read_first>

<action>
Add routes `/page/:slug` and `/dashboard` with correct imports.
</action>

<verify>
<automated>cd web && grep -q "/page/:slug" src/routes/router.tsx && grep -q "/dashboard" src/routes/router.tsx</automated>
</verify>

<done>
Router updated.
</done>

---

## Task 4: Create Tests

<read_first>
- web/src/pages/Page.tsx
- web/src/hooks/useWebSocket.ts
</read_first>

<action>
Create `web/src/__tests__/PageEditor.test.tsx` (view/edit/save tests) and `web/src/__tests__/useWebSocket.test.ts` (connection/reconnect tests).
</action>

<verify>
<automated>cd web && ls src/__tests__/PageEditor.test.tsx src/__tests__/useWebSocket.test.ts 2>&1</automated>
</verify>

<done>
Tests created.
</done>

</tasks>

<verification>
- `cd web && npm run build` exits 0
</verification>

*Phase: 03-03-react-frontend | Plan: 08 - Integration*
