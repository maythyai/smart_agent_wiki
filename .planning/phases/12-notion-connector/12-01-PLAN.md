---
phase: 12-notion-connector
plan: 01
type: execute
wave: 1
depends_on: [10-connector-framework, 11-sync-engine]
files_modified:
  - src/saw/connectors/notion/__init__.py
  - src/saw/connectors/notion/connector.py
  - src/saw/connectors/notion/models.py
  - src/saw/connectors/notion/oauth.py
  - src/saw/connectors/notion/database_selector.py
  - src/saw/db/notion_models.py
  - tests/unit/test_notion_connector.py
autonomous: true
requirements: [NOTI-01, NOTI-02, NOTI-09, NOTI-10]
user_setup:
  - service: notion
    why: "Notion workspace connection via OAuth"
    env_vars:
      - name: NOTION_CLIENT_ID
        source: "Notion Developers -> My integrations -> New integration"
      - name: NOTION_CLIENT_SECRET
        source: "Notion Developers -> My integrations -> Integration secret"
    dashboard_config:
      - task: "Create integration and get credentials"
        location: "https://www.notion.so/my-integrations"

must_haves:
  truths:
    - "User can initiate Notion OAuth flow from Web UI"
    - "User can see list of accessible Notion databases after OAuth"
    - "User can select which databases to sync"
    - "Connector respects Notion 3 req/s rate limit"
    - "Sync cursor persists after each sync for resume capability"
  artifacts:
    - path: "src/saw/connectors/notion/connector.py"
      provides: "NotionConnector implementation"
      exports: ["NotionConnector"]
    - path: "src/saw/connectors/notion/models.py"
      provides: "Notion API models"
      exports: ["NotionPage", "NotionDatabase", "NotionProperty"]
    - path: "src/saw/connectors/notion/oauth.py"
      provides: "Notion OAuth specifics"
      exports: ["NotionOAuthHandler"]
    - path: "src/saw/db/notion_models.py"
      provides: "SQLAlchemy models for Notion sync state"
      exports: ["NotionSyncCursorModel", "NotionDatabaseConfigModel"]
  key_links:
    - from: "NotionConnector"
      to: "UnifiedConnectorInterface"
      via: "Protocol implementation"
      pattern: "class NotionConnector.*UnifiedConnectorInterface"
    - from: "NotionOAuthHandler"
      to: "OAuthHandler"
      via: "Extension for Notion specifics"
    - from: "NotionConnector"
      to: "RateLimitManager"
      via: "3 req/s token bucket"
      pattern: "rate_limiter\\.acquire"
---

<objective>
Implement Notion connector core with OAuth workspace connection, database selection, rate limiting, and sync cursor persistence.

Purpose: Enable users to connect their Notion workspace and select databases for bidirectional sync with SAW.
Output: Working NotionConnector implementing UnifiedConnectorInterface with OAuth flow and database selection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Phase 10 deliverables -->
@src/saw/connectors/protocol.py
@src/saw/connectors/models.py
@src/saw/connectors/oauth_handler.py
@src/saw/connectors/rate_limiter.py
@src/saw/connectors/registry.py
@src/saw/connectors/base_connector.py

<!-- Phase 11 deliverables -->
@src/saw/connectors/sync_engine.py
@src/saw/connectors/sync_status.py
@src/saw/connectors/conflict_resolver.py

<interfaces>
<!-- Key interfaces from Phase 10 that NotionConnector must implement -->

From src/saw/connectors/protocol.py:
```python
@runtime_checkable
class UnifiedConnectorInterface(Protocol):
    @property
    def platform_name(self) -> str: ...
    @property
    def supports_push(self) -> bool: ...
    async def authenticate(self, credentials: dict) -> AuthResult: ...
    async def get_items(self, since: datetime | None = None, filters: dict | None = None) -> list[ConnectorItem]: ...
    async def put_item(self, item: ConnectorItem) -> str: ...
    async def delete_item(self, item_id: str) -> bool: ...
    def transform_to_claim(self, item: ConnectorItem) -> dict: ...
    def transform_from_claim(self, claim: dict) -> ConnectorItem: ...
```

From src/saw/connectors/oauth_handler.py:
```python
@classmethod
def notion(cls, client_id: str, client_secret: str, redirect_uri: str) -> "OAuthConfig":
    return cls(
        client_id=client_id,
        client_secret=client_secret,
        authorize_url="https://api.notion.com/v1/oauth/authorize",
        token_url="https://api.notion.com/v1/oauth/token",
        redirect_uri=redirect_uri,
    )
```

From src/saw/connectors/rate_limiter.py:
```python
class PlatformRateLimit:
    NOTION = RateLimitConfig(requests_per_second=3, burst=5)
```

From src/saw/connectors/base_connector.py:
```python
class BaseConnector:
    def __init__(self, config: ConnectorConfig, rate_limiter: RateLimitManager):
        self._config = config
        self._rate_limiter = rate_limiter

    async def _with_rate_limit(self, operation: Callable) -> Any:
        await self._rate_limiter.acquire(self.platform_name)
        return await operation()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Create Notion models and database schema</name>
<files>
src/saw/connectors/notion/__init__.py,
src/saw/connectors/notion/models.py,
src/saw/db/notion_models.py,
tests/unit/test_notion_models.py
</files>
<behavior>
- Test 1: NotionPage dataclass accepts all required fields (id, title, properties, created_time, last_edited_time, url, parent)
- Test 2: NotionDatabase dataclass includes id, title, properties schema, and is_selected flag
- Test 3: NotionProperty discriminated union handles all Notion types (title, rich_text, number, select, multi_select, date, checkbox, url, email, phone, files, relation, rollup, created_time, last_edited_time, created_by, last_edited_by)
- Test 4: NotionSyncCursorModel persists cursor_token, database_id, last_sync_at
- Test 5: NotionDatabaseConfigModel persists selected databases with sync preferences
</behavior>
<action>
Create Pydantic models for Notion API responses and SQLAlchemy models for sync state persistence.

1. Create `src/saw/connectors/notion/__init__.py` with exports.

2. Create `src/saw/connectors/notion/models.py`:
   - `NotionUser`: id, name, avatar_url, type (person/bot)
   - `NotionFile`: name, type (file/external), url, expiry_time
   - `NotionRichText`: type (text/mention/equation), plain_text, annotations, href
   - `NotionProperty`: Discriminated union using `Annotated` with tags for each property type:
     - TitleProperty, RichTextProperty, NumberProperty (number_format enum)
     - SelectProperty (options list), MultiSelectProperty
     - DateProperty (date range), CheckboxProperty, URLProperty
     - EmailProperty, PhoneProperty, FilesProperty
     - RelationProperty (database_id, synced_property_name), RollupProperty
     - CreatedTimeProperty, LastEditedTimeProperty
     - CreatedByProperty, LastEditedByProperty
   - `NotionPage`: id, parent (database_id or page_id), properties dict, created_time, last_edited_time, url, archived
   - `NotionDatabase`: id, title (rich_text list), properties schema, description, url
   - `NotionSearchResult`: results list, has_more, next_cursor
   - `NotionErrorResponse`: code, message

3. Create `src/saw/db/notion_models.py`:
   - `NotionSyncCursorModel`:
     - id (UUID primary key)
     - connector_id (FK to connector_configs)
     - database_id (Notion database ID)
     - cursor_token (pagination cursor, nullable)
     - last_sync_at (datetime)
     - last_page_edited_at (datetime, for incremental sync)
     - items_synced (int, running count)
   - `NotionDatabaseConfigModel`:
     - id (UUID primary key)
     - connector_id (FK to connector_configs)
     - database_id (Notion database ID)
     - database_name (cached title)
     - is_selected (bool, default True)
     - sync_direction (enum: pull/push/bidirectional)
     - property_mapping (JSON, custom field mappings)
     - created_at, updated_at timestamps

4. Register models in `src/saw/db/__init__.py`.

Use Pydantic v2 with `ConfigDict(from_attributes=True)` for ORM mode.
All datetime fields use UTC timezone.
</action>
<verify>
<automated>pytest tests/unit/test_notion_models.py -v && python -c "from saw.connectors.notion.models import NotionPage, NotionDatabase, NotionProperty; from saw.db.notion_models import NotionSyncCursorModel, NotionDatabaseConfigModel"</automated>
</verify>
<done>
- All model classes instantiate with valid data
- SQLAlchemy models create tables without errors
- Pydantic validation rejects malformed Notion responses
- Discriminated union dispatches to correct property type
</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Implement NotionConnector core</name>
<files>
src/saw/connectors/notion/connector.py,
src/saw/connectors/notion/oauth.py,
tests/unit/test_notion_connector.py
</files>
<behavior>
- Test 1: NotionConnector.platform_name returns "notion"
- Test 2: NotionConnector.supports_push returns True
- Test 3: authenticate() exchanges OAuth code for AuthResult with workspace info
- Test 4: get_items() returns list of ConnectorItem from selected databases
- Test 5: get_items(since=dt) only returns pages edited after dt
- Test 6: Rate limiter enforces 3 req/s (notion-client handles this internally)
- Test 7: Sync cursor persists after get_items() for resume
</behavior>
<action>
Implement NotionConnector following UnifiedConnectorInterface Protocol.

1. Create `src/saw/connectors/notion/oauth.py`:
   - `NotionOAuthHandler` class extending OAuthHandler patterns:
     - `exchange_code()` override for Notion-specific response parsing
     - Notion returns `workspace_id` and `workspace_name` in token response
     - Store workspace info in connector config
   - Handle Notion OAuth errors (invalid_grant, access_denied)

2. Create `src/saw/connectors/notion/connector.py`:
   - `NotionConnector` class implementing UnifiedConnectorInterface:
     - Inherits from BaseConnector for rate limiting integration
     - Uses `notion-client` SDK (Client from notion_client)
     - `platform_name = "notion"`
     - `supports_push = True`

   - `__init__(config: ConnectorConfig, rate_limiter: RateLimitManager, session: AsyncSession)`:
     - Initialize notion_client.AsyncClient with access_token from config
     - Load selected databases from NotionDatabaseConfigModel
     - Load sync cursors from NotionSyncCursorModel

   - `async def authenticate(credentials: dict) -> AuthResult`:
     - Accepts `{"code": str, "state": str}`
     - Uses NotionOAuthHandler to exchange code for tokens
     - Returns AuthResult with access_token, workspace_id in raw_response

   - `async def get_items(since: datetime | None, filters: dict | None) -> list[ConnectorItem]`:
     - Iterate over selected databases from NotionDatabaseConfigModel
     - For each database, query pages with filter:
       - If since provided: `last_edited_time > since.isoformat()`
       - Else: query all pages (initial sync)
     - Use database cursor from NotionSyncCursorModel for pagination
     - Handle has_more and next_cursor for complete sync
     - Respect 3 req/s rate limit (notion-client has built-in limiter, verify)
     - Transform each page to ConnectorItem
     - Update sync cursor after each successful page fetch
     - Return all items as list[ConnectorItem]

   - `async def put_item(item: ConnectorItem) -> str`:
     - Create or update page in Notion
     - Map ConnectorItem.metadata to Notion properties
     - Return Notion page ID

   - `async def delete_item(item_id: str) -> bool`:
     - Archive page in Notion (Notion uses archive, not delete)
     - Return True if archived, False if not found

   - `def transform_to_claim(item: ConnectorItem) -> dict`:
     - Placeholder for Plan 12-02 (detailed transformation)
     - Basic mapping: title -> content preview, metadata -> metadata

   - `def transform_from_claim(claim: dict) -> ConnectorItem`:
     - Placeholder for Plan 12-02
     - Basic mapping: claim content -> title

   - `async def list_databases() -> list[NotionDatabase]`:
     - Use Notion search API with filter: {"property": "object", "value": "database"}
     - Return list of databases user has access to
     - Cache in memory for session duration

3. Register NotionConnector in ConnectorRegistry with "notion" key.

SDK usage pattern:
```python
from notion_client import AsyncClient

client = AsyncClient(auth=access_token)
response = await client.databases.query(
    database_id=database_id,
    filter={"timestamp": "last_edited_time", "last_edited_time": {"after": since.isoformat()}},
    start_cursor=cursor_token,
)
```

Rate limiting: notion-client 3.0.0 has built-in rate limiting. Configure:
```python
client = AsyncClient(auth=access_token, timeout_ms=30000)
# SDK respects 3 req/s automatically
```
</action>
<verify>
<automated>pytest tests/unit/test_notion_connector.py -v -k "test_connector or test_authenticate or test_get_items"</automated>
</verify>
<done>
- NotionConnector implements all UnifiedConnectorInterface methods
- OAuth flow completes with workspace info captured
- get_items() returns pages from selected databases
- Sync cursor persists after each sync operation
- Rate limiting enforced (notion-client built-in)
</done>
</task>

<task type="auto" tdd="true">
<name>Task 3: Implement database selection and sync cursor persistence</name>
<files>
src/saw/connectors/notion/database_selector.py,
src/saw/api/notion.py,
tests/unit/test_database_selector.py,
tests/unit/test_notion_api.py
</files>
<behavior>
- Test 1: list_accessible_databases() returns databases from Notion search
- Test 2: select_databases() persists selection to NotionDatabaseConfigModel
- Test 3: get_selected_databases() returns only selected databases
- Test 4: Sync cursor updates after each page fetch in get_items()
- Test 5: Sync resume continues from last cursor position
</behavior>
<action>
Create database selection logic and sync cursor management.

1. Create `src/saw/connectors/notion/database_selector.py`:
   - `DatabaseSelector` class:
     - `__init__(client: AsyncClient, session: AsyncSession, connector_id: str)`

     - `async def list_accessible_databases() -> list[NotionDatabase]`:
       - Use Notion search API: `client.search(filter={"property": "object", "value": "database"})`
       - Paginate through all results (has_more, next_cursor)
       - Return list with id, title (extracted from rich_text), properties schema

     - `async def select_databases(database_ids: list[str]) -> None`:
       - Validate each database_id exists (optional, catch errors gracefully)
       - Clear previous selections for this connector
       - Insert new NotionDatabaseConfigModel entries with is_selected=True
       - Commit to database

     - `async def get_selected_databases() -> list[NotionDatabaseConfigModel]`:
       - Query NotionDatabaseConfigModel where connector_id matches and is_selected=True
       - Return list for use in get_items()

     - `async def update_property_mapping(database_id: str, mapping: dict) -> None`:
       - Update property_mapping JSON in NotionDatabaseConfigModel
       - For custom Notion property -> SAW field mappings

2. Create `src/saw/api/notion.py` (FastAPI endpoints):
   - `GET /api/v1/connectors/notion/databases`:
     - List accessible Notion databases for authenticated user
     - Requires active Notion connector
     - Returns: `{"databases": [{"id": str, "title": str, "properties": dict}]}`

   - `POST /api/v1/connectors/notion/databases/select`:
     - Body: `{"database_ids": list[str]}`
     - Persist database selection
     - Returns: `{"selected": int, "total_accessible": int}`

   - `GET /api/v1/connectors/notion/databases/selected`:
     - Returns currently selected databases with sync status

   - `PATCH /api/v1/connectors/notion/databases/{database_id}/mapping`:
     - Update property mapping for specific database
     - Body: `{"property_mapping": dict}`

3. Sync cursor persistence (in NotionConnector.get_items):
   - After each successful `databases.query` call:
     - Save `start_cursor` used (for resume)
     - Save `next_cursor` if has_more
     - Update `last_sync_at` to current time
     - Update `last_page_edited_at` to max edited_time in batch
     - Increment `items_synced` by batch count
   - Use upsert pattern for NotionSyncCursorModel

4. Sync resume logic (in NotionConnector.get_items):
   - On start, check NotionSyncCursorModel for cursor_token
   - If cursor exists and sync was interrupted (no completion marker):
     - Resume from cursor_token instead of since timestamp
   - After complete sync of a database, clear cursor_token

Error handling:
- Database no longer accessible: mark as error, continue with others
- Property schema changed: note in log, continue with best-effort mapping
- Rate limit hit: exception propagates to RetryHandler
</action>
<verify>
<automated>pytest tests/unit/test_database_selector.py tests/unit/test_notion_api.py -v</automated>
</verify>
<done>
- User can list accessible Notion databases via API
- User can select databases for sync via API
- Selected databases persist across sessions
- Sync cursor persists after each page fetch
- Interrupted sync resumes from last cursor position
- API endpoints return correct responses
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → OAuth | User authorizes SAW to access Notion workspace |
| SAW → Notion API | SAW makes authenticated requests to Notion |
| Notion → SAW OAuth | Notion redirects with authorization code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Spoofing | OAuth callback | mitigate | Validate state parameter matches stored OAuth state (10-min TTL) |
| T-12-02 | Tampering | Sync cursor | mitigate | Cursor stored server-side, not client-controlled |
| T-12-03 | Information Disclosure | OAuth tokens | mitigate | Tokens encrypted at rest via Fernet (Phase 10-02) |
| T-12-04 | Information Disclosure | API responses | mitigate | Tokens masked in logs/responses showing last 4 chars only (AUTH-04) |
| T-12-05 | Denial of Service | Rate limiting | accept | Notion enforces 3 req/s; SAW respects via SDK built-in limiter |
| T-12-06 | Elevation of Privilege | Database access | mitigate | Only query databases shared with integration; OAuth scopes do not request extra permissions |
</threat_model>

<verification>
1. All unit tests pass (models, connector, database selector, API)
2. NotionConnector implements UnifiedConnectorInterface (runtime_checkable)
3. OAuth flow completes end-to-end with mock Notion API
4. Rate limiting verified (notion-client SDK handles)
5. Sync cursor persists and resumes correctly
</verification>

<success_criteria>
- [ ] User can initiate Notion OAuth flow and complete authorization
- [ ] System captures workspace_id and workspace_name from OAuth response
- [ ] User can list accessible Notion databases via API endpoint
- [ ] User can select databases for sync, persisted to database
- [ ] NotionConnector.get_items() returns pages from selected databases
- [ ] Sync cursor persists after each successful page fetch
- [ ] Interrupted sync resumes from last cursor position
- [ ] Rate limiting enforced (3 req/s via notion-client SDK)
- [ ] All tokens encrypted at rest (Fernet from Phase 10)
</success_criteria>

<output>
After completion, create `.planning/phases/12-notion-connector/12-01-SUMMARY.md`
</output>
