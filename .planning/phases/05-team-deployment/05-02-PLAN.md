---
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/saw/auth/__init__.py
  - src/saw/auth/models.py
  - src/saw/auth/jwt.py
  - src/saw/auth/dependencies.py
  - src/saw/auth/routes.py
  - src/saw/api/server.py
  - requirements.txt
requirements:
  - TEAM-04
  - TEAM-05
autonomous: true
---

# Plan 05-02: User Authentication System

**Objective:** 实现用户注册、登录和 JWT 认证系统

## Tasks

### Task 1: Create Authentication Module

<read_first>
- src/saw/db/models.py (User 模型)
- requirements.txt (依赖)
</read_first>

<action>
创建 `src/saw/auth/` 目录：

1. 创建 `src/saw/auth/__init__.py`

2. 创建 `src/saw/auth/models.py`：
   ```python
   from pydantic import BaseModel, EmailStr
   from enum import Enum

   class UserRole(str, Enum):
       ADMIN = "admin"
       EDITOR = "editor"
       VIEWER = "viewer"

   class UserCreate(BaseModel):
       email: EmailStr
       password: str

   class UserLogin(BaseModel):
       email: EmailStr
       password: str

   class UserResponse(BaseModel):
       id: str
       email: str
       role: UserRole
       is_active: bool

   class Token(BaseModel):
       access_token: str
       refresh_token: str
       token_type: str = "bearer"

   class TokenPayload(BaseModel):
       sub: str  # user_id
       exp: int
       iat: int
   ```

3. 添加依赖到 `requirements.txt`：
   ```
   python-jose[cryptography]>=3.3.0  # JWT
   passlib[bcrypt]>=1.7.4  # 密码哈希
   pydantic[email]>=2.0.0
   ```
</action>

<acceptance_criteria>
- src/saw/auth/__init__.py 存在
- src/saw/auth/models.py 包含 `UserRole` 枚举
- src/saw/auth/models.py 包含 `UserCreate` 和 `UserLogin` Pydantic 模型
- requirements.txt 包含 `python-jose`
- requirements.txt 包含 `passlib`
</acceptance_criteria>

---

### Task 2: Implement JWT Token Management

<read_first>
- src/saw/auth/models.py (Token 模型)
- src/saw/config.py (配置)
</read_first>

<action>
创建 `src/saw/auth/jwt.py`：

1. 实现 `create_access_token()` 函数：
   ```python
   def create_access_token(user_id: str, expires_delta: timedelta) -> str:
       payload = {
           "sub": user_id,
           "exp": datetime.utcnow() + expires_delta,
           "iat": datetime.utcnow(),
       }
       return jwt.encode(payload, SECRET_KEY, algorithm=ALGORITHM)
   ```

2. 实现 `create_refresh_token()` 函数：
   ```python
   def create_refresh_token(user_id: str) -> str:
       # 7 天有效期
       expires = timedelta(days=7)
       return create_access_token(user_id, expires)
   ```

3. 实现 `decode_token()` 函数：
   ```python
   def decode_token(token: str) -> TokenPayload:
       payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
       return TokenPayload(**payload)
   ```

4. 实现 `verify_password()` 和 `hash_password()` 函数：
   ```python
   from passlib.context import CryptContext
   pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

   def hash_password(password: str) -> str:
       return pwd_context.hash(password)

   def verify_password(plain: str, hashed: str) -> bool:
       return pwd_context.verify(plain, hashed)
   ```

5. 添加配置项：
   ```python
   @dataclass
   class AuthConfig:
       secret_key: str = "change-me-in-production"
       algorithm: str = "HS256"
       access_token_expire_minutes: int = 30
       refresh_token_expire_days: int = 7
   ```
</action>

<acceptance_criteria>
- jwt.py 包含 `create_access_token()` 函数
- jwt.py 包含 `create_refresh_token()` 函数
- jwt.py 包含 `decode_token()` 函数
- jwt.py 包含 `hash_password()` 函数
- jwt.py 包含 `verify_password()` 函数
- config.py 包含 `AuthConfig` 数据类
- tests 包含 `test_create_and_decode_token` 测试函数
- tests 包含 `test_hash_and_verify_password` 测试函数
</acceptance_criteria>

---

### Task 3: Create Authentication Dependencies

<read_first>
- src/saw/auth/jwt.py (JWT 函数)
- src/saw/db/session.py (数据库会话)
</read_first>

<action>
创建 `src/saw/auth/dependencies.py`：

1. 实现 `get_current_user()` 依赖：
   ```python
   from fastapi import Depends, HTTPException, status
   from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials

   security = HTTPBearer()

   async def get_current_user(
       credentials: HTTPAuthorizationCredentials = Depends(security),
       session: AsyncSession = Depends(get_session)
   ) -> User:
       token = credentials.credentials
       try:
           payload = decode_token(token)
           user = await session.get(User, payload.sub)
           if not user or not user.is_active:
               raise HTTPException(status_code=401, detail="Invalid user")
           return user
       except JWTError:
           raise HTTPException(status_code=401, detail="Invalid token")
   ```

2. 实现 `get_current_active_user()` 依赖：
   ```python
   async def get_current_active_user(
       user: User = Depends(get_current_user)
   ) -> User:
       if not user.is_active:
           raise HTTPException(status_code=403, detail="Inactive user")
       return user
   ```

3. 实现角色检查依赖：
   ```python
   def require_role(*roles: UserRole):
       async def role_checker(
           user: User = Depends(get_current_active_user)
       ) -> User:
           if UserRole(user.role) not in roles:
               raise HTTPException(status_code=403, detail="Insufficient permissions")
           return user
       return role_checker
   ```
</action>

<acceptance_criteria>
- dependencies.py 包含 `get_current_user()` 函数
- dependencies.py 包含 `get_current_active_user()` 函数
- dependencies.py 包含 `require_role()` 函数
- tests 包含 `test_get_current_user_valid_token` 测试函数
- tests 包含 `test_get_current_user_invalid_token` 测试函数
- tests 包含 `test_require_role_admin_only` 测试函数
</acceptance_criteria>

---

### Task 4: Create Authentication API Routes

<read_first>
- src/saw/auth/models.py (Pydantic 模型)
- src/saw/auth/jwt.py (JWT 函数)
- src/saw/auth/dependencies.py (依赖)
</read_first>

<action>
创建 `src/saw/auth/routes.py`：

1. 实现注册端点：
   ```python
   @router.post("/register", response_model=UserResponse)
   async def register(
       user_data: UserCreate,
       session: AsyncSession = Depends(get_session)
   ):
       # 检查邮箱是否已存在
       existing = await session.execute(
           select(User).where(User.email == user_data.email)
       )
       if existing.scalar():
           raise HTTPException(status_code=400, detail="Email already registered")

       # 创建用户
       user = User(
           id=str(uuid4()),
           email=user_data.email,
           hashed_password=hash_password(user_data.password),
           role=UserRole.VIEWER.value,
       )
       session.add(user)
       await session.commit()
       return user
   ```

2. 实现登录端点：
   ```python
   @router.post("/login", response_model=Token)
   async def login(
       user_data: UserLogin,
       session: AsyncSession = Depends(get_session)
   ):
       user = await session.execute(
           select(User).where(User.email == user_data.email)
       )
       user = user.scalar()
       if not user or not verify_password(user_data.password, user.hashed_password):
           raise HTTPException(status_code=401, detail="Invalid credentials")

       # 更新最后登录时间
       user.last_login = datetime.utcnow()
       await session.commit()

       return Token(
           access_token=create_access_token(user.id, timedelta(minutes=30)),
           refresh_token=create_refresh_token(user.id),
       )
   ```

3. 实现刷新令牌端点：
   ```python
   @router.post("/refresh", response_model=Token)
   async def refresh_token(
       user: User = Depends(get_current_user)
   ):
       return Token(
           access_token=create_access_token(user.id, timedelta(minutes=30)),
           refresh_token=create_refresh_token(user.id),
       )
   ```

4. 实现获取当前用户端点：
   ```python
   @router.get("/me", response_model=UserResponse)
   async def get_me(user: User = Depends(get_current_active_user)):
       return user
   ```

5. 在 `src/saw/api/server.py` 注册路由：
   ```python
   from saw.auth.routes import router as auth_router
   app.include_router(auth_router, prefix="/api/v1/auth", tags=["auth"])
   ```
</action>

<acceptance_criteria>
- routes.py 包含 `POST /register` 端点
- routes.py 包含 `POST /login` 端点
- routes.py 包含 `POST /refresh` 端点
- routes.py 包含 `GET /me` 端点
- server.py 包含认证路由注册
- tests 包含 `test_register_success` 测试函数
- tests 包含 `test_login_success` 测试函数
- tests 包含 `test_login_invalid_password` 测试函数
</acceptance_criteria>

---

## Verification Criteria

1. **User registration**: New user can register with email and password
2. **User login**: Registered user can login and receive tokens
3. **Token refresh**: Refresh token generates new access token
4. **Protected routes**: Routes with `require_role()` reject unauthorized users

## must_haves

- 用户注册/登录功能完整
- JWT Token 生成和验证
- 角色检查依赖
- API 路由注册

## truths

- 密码使用 bcrypt 哈希
- Access token 有效期 30 分钟
- Refresh token 有效期 7 天
- 默认新用户角色为 viewer