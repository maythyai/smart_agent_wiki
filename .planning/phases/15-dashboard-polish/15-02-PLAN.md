---
phase: 15-dashboard-polish
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - docs/integrations/overview.md
  - docs/integrations/notion.md
  - docs/integrations/logseq.md
  - docs/integrations/slack.md
  - docs/integrations/discord.md
  - docs/integrations/feishu.md
  - docs/integrations/wecom.md
  - docs/integrations/github.md
  - tests/integration/test_all_connectors.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "All connector documentation is complete"
    - "User can follow step-by-step guide to connect each platform"
    - "Integration tests verify all connectors work together"
    - "Cross-connector sync does not interfere with each other"
    - "Webhook routing delivers events to correct handlers"
  artifacts:
    - path: "docs/integrations/overview.md"
      provides: "Integration overview and common setup"
      min_lines: 50
    - path: "docs/integrations/notion.md"
      provides: "Notion connector setup guide"
      contains: "OAuth, database selection, property mapping"
    - path: "docs/integrations/github.md"
      provides: "GitHub connector setup guide"
      contains: "OAuth, repository selection, webhook setup"
    - path: "tests/integration/test_all_connectors.py"
      provides: "Integration test suite"
      contains: "dashboard_aggregation, cross_connector_sync, webhook_routing"
  key_links:
    - from: "docs/integrations/overview.md"
      to: "docs/integrations/*.md"
      via: "cross-references"
      pattern: "\\[.*\\]\\(.*\\.md\\)"
    - from: "tests/integration/test_all_connectors.py"
      to: "all connector implementations"
      via: "import and instantiate"
      pattern: "from saw\\.connectors"
---

<objective>
Complete connector documentation and integration testing.

Purpose: Ensure users have comprehensive guides for all platforms and system works reliably.
Output: Documentation for all 7 connectors, integration test suite for cross-connector validation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-dashboard-polish/CONTEXT.md

# Connector Summary (from prior phases)

**Phase 10-01:** UnifiedConnectorInterface Protocol, ConnectorRegistry, RateLimitManager
**Phase 10-02:** OAuthHandler, TokenEncryption (Fernet), TokenRefresh, TokenMasker
**Phase 10-03:** Webhook endpoints, HMAC verification, rate limiting
**Phase 11-01:** SyncEngine, ConflictResolver, SyncLogger
**Phase 11-02:** HealthMonitor, RetryHandler, BackpressureManager
**Phase 11-03:** IM message handling, sync API endpoints
**Phase 12-01~03:** NotionConnector (OAuth, database sync, property mapping, bidirectional)
**Phase 13-01:** LogseqConnector (local file sync, file watching, block parsing)
**Phase 13-02:** SlackConnector (OAuth, Events API, message ingestion)
**Phase 13-03:** DiscordConnector (Gateway WebSocket, message ingestion)
**Phase 13-04:** FeishuConnector, WeComConnector (webhooks, encryption)
**Phase 14-01~03:** GitHubConnector (OAuth/App, Issues/Discussions, webhooks)

# Connector Capabilities Summary

| Platform | Auth | Sync Direction | Special Features |
|----------|------|----------------|------------------|
| Notion | OAuth | Bidirectional | Database selection, property mapping |
| Logseq | Local path | Bidirectional | File watching, block parsing |
| Slack | OAuth | Pull | Events API, thread context |
| Discord | Bot token | Pull | Gateway WebSocket, embeds |
| Feishu | App ID/Secret | Pull | Webhooks, multi-tenant |
| WeCom | Corp ID/Secret | Pull | Webhooks, AES encryption |
| GitHub | OAuth/App | Pull | Issues/Discussions, webhooks |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write connector documentation</name>
  <files>docs/integrations/overview.md, docs/integrations/notion.md, docs/integrations/logseq.md, docs/integrations/slack.md, docs/integrations/discord.md, docs/integrations/feishu.md, docs/integrations/wecom.md, docs/integrations/github.md</files>
  <action>
    Create comprehensive documentation for all 7 connectors. Each doc follows consistent structure.

    1. docs/integrations/overview.md (Integration Overview):
       - Introduction to SAW integrations
       - Dashboard overview (link to dashboard page)
       - Common setup steps (CLI web server, environment variables)
       - Connector comparison table (auth type, sync direction, features)
       - Troubleshooting common issues
       - Links to individual connector guides

    2. docs/integrations/notion.md (Notion Connector Guide):
       - Prerequisites: Notion account, SAW web server running
       - Step 1: Create Notion integration in Notion Developers
       - Step 2: Get OAuth credentials (client_id, client_secret)
       - Step 3: Configure SAW (env vars: NOTION_CLIENT_ID, NOTION_CLIENT_SECRET)
       - Step 4: Connect workspace via /integrations dashboard or CLI
       - Step 5: Select databases to sync
       - Step 6: Configure property mapping (title, content, confidence, freshness)
       - Step 7: Test bidirectional sync
       - Troubleshooting: token expiration, rate limits, property type changes
       - Screenshots: OAuth flow, database selection UI, sync status

    3. docs/integrations/logseq.md (Logseq Connector Guide):
       - Prerequisites: Logseq installed, local graph directory
       - Step 1: Identify Logseq graph path (e.g., ~/logseq/my-graph)
       - Step 2: Configure SAW with graph path (LOGSEQ_GRAPH_PATH env var)
       - Step 3: Start file watching (CLI: saw logseq watch)
       - Step 4: Test sync (edit in Logseq, verify in SAW)
       - Block parsing: property drawers, wikilinks, namespaces
       - Troubleshooting: file permissions, concurrent edits, wikilink preservation

    4. docs/integrations/slack.md (Slack Connector Guide):
       - Prerequisites: Slack workspace admin access
       - Step 1: Create Slack App in API dashboard
       - Step 2: Configure OAuth scopes (channels:history, channels:read)
       - Step 3: Enable Events API (message.channels event)
       - Step 4: Set webhook URL (https://your-saw/api/v1/webhooks/slack)
       - Step 5: Install app to workspace
       - Step 6: Test message ingestion
       - Thread context capture, reaction signals
       - Troubleshooting: event delivery, token rotation, rate limits

    5. docs/integrations/discord.md (Discord Connector Guide):
       - Prerequisites: Discord server admin access
       - Step 1: Create Discord application in Developer Portal
       - Step 2: Create bot, get bot token
       - Step 3: Configure bot permissions (Read Messages, Read Message History)
       - Step 4: Add bot to server (OAuth2 URL generator)
       - Step 5: Configure gateway connection (DISCORD_BOT_TOKEN env var)
       - Step 6: Test message ingestion
       - Embed handling, thread context
       - Troubleshooting: gateway reconnection, rate limits, embed parsing

    6. docs/integrations/feishu.md (Feishu Connector Guide):
       - Prerequisites: Feishu tenant admin access
       - Step 1: Create Feishu app in Developer Console
       - Step 2: Get App ID and App Secret
       - Step 3: Configure webhook event subscriptions
       - Step 4: Set webhook URL and verify signature
       - Step 5: Configure multi-tenant tokens if needed
       - Step 6: Test document sync
       - Chinese content handling
       - Troubleshooting: signature verification, token refresh, encoding

    7. docs/integrations/wecom.md (WeCom Connector Guide):
       - Prerequisites: WeCom enterprise admin access
       - Step 1: Create WeCom application in Admin Console
       - Step 2: Get Corp ID, Agent ID, Secret
       - Step 3: Configure webhook URL with AES encryption key
       - Step 4: Set up message receiving mode
       - Step 5: Test message ingestion
       - AES-256-CBC decryption, message parsing
       - Troubleshooting: encryption errors, message format, encoding

    8. docs/integrations/github.md (GitHub Connector Guide):
       - Prerequisites: GitHub account, repository access
       - Step 1: Choose auth method (OAuth App or GitHub App)
       - Step 2: Create GitHub App/OAuth App in Developer Settings
       - Step 3: Configure callback URL and webhook URL
       - Step 4: Set required permissions (Issues, Discussions, Webhooks)
       - Step 5: Install app on repositories
       - Step 6: Select repositories in SAW dashboard
       - Step 7: Test Issue/Discussion sync
       - Label mapping, comment capture, webhook reconciliation
       - Troubleshooting: webhook delivery, rate limits, GraphQL pagination

    All docs use consistent formatting:
    - Markdown with proper headings
    - Code blocks for CLI commands and env vars
    - Tables for configuration options
    - Links to related docs and API references
    - Chinese language for primary content (project convention)
  </action>
  <verify>
    <automated>find docs/integrations -name "*.md" -exec wc -l {} \\; | awk '{sum+=$1} END {print sum}' && test $(find docs/integrations -name "*.md" | wc -l) -eq 8</automated>
  </verify>
  <done>
    8 documentation files created in docs/integrations/.
    Each doc covers prerequisites, step-by-step setup, troubleshooting.
    Total line count >= 400 (50 lines per doc minimum).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create integration test suite</name>
  <files>tests/integration/test_all_connectors.py</files>
  <behavior>
    - Test 1: dashboard_aggregation returns all registered connectors
    - Test 2: cross_connector_sync runs multiple syncs concurrently without interference
    - Test 3: webhook_routing delivers Slack event to SlackConnector, Discord event to DiscordConnector
    - Test 4: rate_limiting_across_all respects per-platform limits under concurrent load
  </behavior>
  <action>
    Create comprehensive integration tests for all connectors working together.

    1. Create tests/integration/test_all_connectors.py.

    2. Test: dashboard_aggregation:
       - Register mock connectors for all 7 platforms in ConnectorRegistry
       - Call HealthMonitor.get_system_health()
       - Call SyncStatusTracker.get_all_statuses()
       - Verify response includes all platforms with correct structure
       - Verify system_health summary counts correctly

    3. Test: cross_connector_sync:
       - Create mock SyncEngine with concurrent sync simulation
       - Trigger sync for Notion, Slack, GitHub simultaneously (asyncio.gather)
       - Verify each sync completes independently
       - Verify no cross-contamination of sync cursors
       - Verify Write Queue handles items from multiple sources correctly

    4. Test: webhook_routing:
       - Send mock Slack event to /api/v1/webhooks/slack
       - Send mock Discord event to /api/v1/webhooks/discord
       - Send mock GitHub event to /api/v1/webhooks/github
       - Verify each reaches correct connector handler
       - Verify HMAC signature verification passes for each

    5. Test: rate_limiting_across_all:
       - Configure RateLimitManager with limits for all platforms
       - Send concurrent requests (10 per platform)
       - Verify Notion: 3 req/s limit respected
       - Verify GitHub: 5000 req/hr limit respected
       - Verify Slack: tier-based limits respected
       - Verify no request exceeds its platform limit

    6. Test: error_handling_consistency:
       - Simulate transient failures on multiple connectors
       - Verify RetryHandler applies exponential backoff consistently
       - Verify HealthMonitor transitions statuses correctly
       - Verify error messages captured in ConnectorSyncLog

    7. Use pytest-asyncio for async test handling.
    8. Use mock fixtures from tests/conftest.py (extend if needed).
    9. Follow existing test patterns from tests/api/ and tests/connectors/.
  </action>
  <verify>
    <automated>pytest tests/integration/test_all_connectors.py -x -v</automated>
  </verify>
  <done>
    Integration test suite with 5 tests passing.
    All tests verify cross-connector behavior.
    Tests cover dashboard, concurrent sync, webhook routing, rate limiting, error handling.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation reader → Connector | User follows guide to configure connector |
| Integration tests → All connectors | Tests verify system behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Spoofing | Webhook routing | mitigate | HMAC signature verification per connector (existing from Phase 10-03) |
| T-15-06 | Denial of Service | Concurrent sync | mitigate | BackpressureManager limits concurrent operations (existing from Phase 11-02) |
</threat_model>

<verification>
- All documentation files exist with required content
- Integration tests pass
- Cross-connector behavior validated
</verification>

<success_criteria>
1. All connector documentation is complete
2. Integration tests verify all connectors work together
3. Cross-connector sync does not interfere with each other
4. Webhook routing delivers events to correct handlers
</success_criteria>

<output>
After completion, create `.planning/phases/15-dashboard-polish/15-02-SUMMARY.md`
</output>